# 三层世界屎点总览 — 优先级统一排序

> 创建时间：2026-02-23
> 审计来源：Codex (GPT-5) 初审 + Claude Opus 复审 + 3 路 Explore Agent + Codex 逐文件深审
> 详情见：L3-底层引擎地图.md / L2-中层API地图.md / L1-上层Agent地图.md
> **本次更新**：M6 迁移复核（L3 分离完成确认 + 增量屎点重排）

---

## 屎山评分

| 维度 | 评分 | 说明 |
|------|------|------|
| Codex 初审 | 8.4/10 | 高风险，可跑但脆 |
| 阶段 1-4 修复后 | ~6/10 | StateManager/XP散写/死代码已修 |
| 全面审计时 | 6.5/10 | 同步I/O + 层级违规 + 接口碎片 + L1 裸异常洪水 |
| **M5 修复后** | **~5.5/10** | L3 屎点大面积修复（15/19 完成），L2/L1 尚未动 |
| **M6 迁移复核（仅 L3）** | **6.2/10** | 排除已知 P0 断裂后，L3 分离完成但仍有中高强度内部债务 |

---

## 三层健康度一览

| 层 | 模块数 | 评分 | 最大问题 |
|----|--------|------|---------|
| L3 底层引擎 | **47**（原57，M5删9） | **B（结构完成）/ 屎山度 6.2** | 分离完成但存在反向依赖、私有方法耦合、大文件集群 |
| L2 中层 API | 22 | D- (2.5/10) | **不存在**统一接口；MCP 实例分叉（含 combat）；错误治理不足 |
| L1 上层 Agent | 10 | D+ (3.5/10) | 30 处工具直通 L3 + 43 裸异常 + 9 STUB 工具 |

---

## M6 — L3 分离完成复核（Codex）

| 检查项 | 结果 | 证据 |
|--------|------|------|
| 迁移对完整性 | **36/36 完成** | 旧文件删除 + 新路径存在逐对核验通过 |
| 旧路径直引残留 | **0** | `app/world/world_graph.py` 等旧入口无引用 |
| L3 核心回归 | **402 passed** | graph/events/memory/intent/combat 子集全绿 |
| 结论 | **分离完成** | 结构已收口，剩余为“实现债务”而非“迁移遗漏” |

---

## P0 — 立即修复（阻塞性 / 数据丢失风险）

| # | 屎点 | 层 | 位置 | 工作量 | 影响 | 状态 |
|---|------|---|------|--------|------|------|
| 1 | `settings.admin_agentic_thinking` 字段不存在 | L2 | `pipeline_orchestrator.py:858` | **1 行** | GM observer 静默失败 | **✅ 已修复**（→ `settings.admin_flash_thinking_level`） |
| 2 | async 里 50 处同步 Firestore I/O | L3 | 多文件 | **大** | 高并发阻塞，SSE 卡死 | **✅ 全部消除**（M3删8 + M4 to_thread 11 + M5删31） |
| 3 | LLMService 7 处同步 Gemini API 调用 | L1 | `llm_service.py:197,431,491,548,599,732,898` | **中** | LLM 调用阻塞事件循环 | **✅ 已修复**（7 处 sync→async） |
| 4 | IntentExecutor 13 处 `except Exception` | L3 | `app/world/intent/executor.py` 多处 | **小** | 完全无法调试 | **✅ 已修复**（具体异常+logger） |
| 5 | **MCP 工具实例分叉** | L2 | `mcp/tools/*.py` 5+ 文件 | **中** | MCP 和主流程缓存完全隔离，数据不一致 | **🔍 已审计，挂起**（详见 Firestore残留Store迁移清单.md，含 T1-T13 执行计划） |
| 6 | **InstanceManager key 无 session_id** | L3 | `instance_manager.py:135` | **小** | 跨会话 NPC 上下文泄漏 | **✅ 已修复**（session_id 隔离） |
| 7 | 队友战斗工具 `choose_battle_action` 重名冲突 | L1 | `immersive_tools.py:571`, `teammate_response_service.py:51,1002` | **小** | 工具选择不确定，战斗行为漂移 | **✅ 已修复**（删除 stub） |
| 8 | **GmExtraTools 调 SessionRuntime 私有方法** | L1 | `gm_extra_tools.py:274-275` (`_sync_tick_to_narrative`, `_apply_tick_side_effects`) | **中** | SessionRuntime 重构时静默断裂 | **✅ 已修复**（提取公共 apply_tick_results） |

---

## P1 — 本周修复（功能正确性 / 数据一致性）

| # | 屎点 | 层 | 位置 | 工作量 | 影响 | 状态 |
|---|------|---|------|--------|------|------|
| 9 | PipelineOrchestrator 11 处裸异常 | L2 | `pipeline_orchestrator.py` 多处 | 小 | 关键操作静默失败 | |
| 10 | SessionRuntime 恢复缺 Firestore 回读 | L3 | `session_runtime.py:261-278` | 小 | 重启后丢失状态 | 挂起（等重建） |
| 11 | PartyService 先改内存再落库，无锁 | L3 | `party_service.py:62,147,165,212` | 中 | 并发数据不一致 | 挂起（Store 已删） |
| 12 | WorldGraph 脏标记不完整 | L3 | `world_graph.py` + `behavior_engine.py` | 中 | 修改不保存 | **✅ 已修复**（merge_state + behavior 包装方法） |
| 13 | GameRuntime 同 world 首次访问无锁初始化竞态 | L3 | `game_runtime.py:44-49` | 小 | 并发首次访问可能重复 `initialize()` | **✅ 已修复**（per-world 双检锁） |
| 14 | **IntentExecutor MOVE 先改时间再切图** | L3 | `app/world/intent/executor.py:171-183` | 中 | 切图失败后时间已被污染 | **✅ 已修复**（先切图后推时间） |
| 15 | **IntentExecutor USE_ITEM 先加血再扣物品** | L3 | `app/world/intent/executor.py:730-753` | 中 | 扣除失败→免费回血漏洞 | **✅ 已修复**（先扣物品后加血） |
| 16 | **NarrativeService 同步 I/O + 无锁 reload** | L3 | `narrative_service.py:74,99,155` | 中 | 阻塞 + 并发竞态 | **✅ 已修复**（to_thread + per-world 锁） |
| 17 | **PasserbyService 同步 I/O + memory-first + 缓存治理缺失** | L2 | `passerby_service.py:84,85,92,276,328` | 中 | 阻塞 + 状态不一致 + 长驻缓存风险 | |
| 18 | **EventService 直调 LLM + 无事务写入** | L2 | `event_service.py:42-45,93-104` | 中 | 层级违规 + 半写风险 | |
| 19 | GM Extra 9 处裸异常 | L1 | `gm_extra_tools.py` L83,L112,L159,L187,L219,L244,L276,L359,L385 | 小 | 工具调用静默失败 | |
| 20 | `choose_combat_action` 直接操作 L3 + 私有属性 | L1 | `gm_extra_tools.py:241,249-251` | 中 | L1 工具直操 L3 internal | |
| 21 | NPC 实例无持久化 | L3 | `instance_manager.py:83` | 中 | 重启丢失 NPC 记忆 | **✅ 已修复**（flush_all + shutdown hook） |
| 22 | 队友 `model_config_override` 未生效（硬编码模型） | L1 | `teammate_response_service.py:1099-1100`, `models/party.py:60` | 小 | 难以按角色差异调模 | |
| 23 | **LLMService 13 处 print() 代替 logging** | L1 | `llm_service.py` 多处 | 小 | 生产环境错误不可见 | |
| 24 | **MemoryGraphizer 非原子图谱写入** | L1 | `memory_graphizer.py:509-638` | 中 | 部分写入→图谱损坏 | |
| 25 | **TeammateResponseService 静默全轮失败** | L1 | `teammate_response_service.py:421,876` | 小 | 队友错误被完全吞咽 | |
| 26 | **TieredAIService 静默层级降级** | L1 | `tiered_ai_service.py:103,130` | 小 | Main NPC 不可见退化为路人品质 | |

---

## P1.5 — M6 增量审计（L3 分离后新增识别）

| ID | 屎点 | 层 | 位置 | 工作量 | 影响 | 状态 |
|----|------|---|------|--------|------|------|
| M6-1 | 条件解析失败默认放行（`satisfied=True`） | L3 | `app/world/events/behavior_engine.py:130-141` | 小 | 事件可能误触发/误推进 | 新增 |
| M6-2 | L3 反向依赖 L1（`InstanceManager -> MemoryGraphizer`） | L3 | `app/world/npc/instance_manager.py:403` | 中 | 层边界打穿，测试与部署耦合扩大 | 新增 |
| M6-3 | IntentExecutor 直接调用 SessionRuntime 私有方法 | L3 | `app/world/intent/executor.py:210-232` | 中 | Runtime 重构时高概率静默断裂 | 新增 |
| M6-4 | 战斗模板加载处于显式挂起（`NotImplementedError`） | L3 | `app/world/combat/enemy_registry.py:320` | 中 | World 模板加载链路不可用（受控断点） | 挂起（专项） |
| M6-5 | L3 超大文件集群（5 个 > 1k 行） | L3 | `combat_engine.py`, `narrative_service.py`, `builder.py`, `behavior_engine.py`, `world_graph.py` | 大 | 变更成本高，回归面大 | 挂起（拆分专项） |
| M6-6 | Runtime 残留已删模块硬依赖（`area_runtime/companion_*`） | L3/混合 | `app/runtime/session_runtime.py:54,353,395,647` + `app/runtime/event_machine.py:178,265,346,847` | 中 | 构造/运行链路依赖旧模块，重构收口风险高 | 已识别（专项收口） |
| M6-7 | player 子系统残留旧 Store 依赖（`CharacterStore`） | L3/混合 | `app/world/player/ability_check.py:6`, `app/world/player/character.py:16` | 中 | 角色/检定链路无法彻底脱离旧存储层 | 已识别（Store迁移专项） |
| M6-8 | Snapshot 恢复直接写 WorldGraph 私有字段 `_sealed` | L3 | `app/world/graph/snapshot.py:292,372` | 小 | 破坏封装边界，后续图容器改造易断 | 新增 |
| M6-9 | Intent/Runtime 仍有多处广义异常吞咽 | L3/混合 | `app/world/intent/executor.py:174,212,233,330,391,478` + `app/runtime/session_runtime.py:552,564,594` | 小 | 错误可见性不足，定位成本高 | 新增 |

---

## P2 — 两周内修复（安全 / 内存 / 可维护性）

| # | 屎点 | 层 | 位置 | 工作量 | 影响 | 状态 |
|---|------|---|------|--------|------|------|
| 27 | 25+ 处 API 异常泄露到客户端 | L2 | `game_v2.py`, `main.py` | 中 | 信息泄露 | |
| 28 | SessionHistoryManager 缓存生命周期缺失 | L3 | `session_history.py:353-389` | 小 | 长运行内存增长 | **✅ 已修复**（OrderedDict + LRU max_sessions=50） |
| 29 | AdminCoordinator 2 个死缓存 | L2 | `admin_coordinator.py:110-111` | 小 | 死代码 | |
| 30 | WorldRuntime 残留 | L2 | `world_runtime.py` | 中 | 恢复语义不一致 | |
| 31 | InstanceManager `_locks` 只增不减 | L3 | `instance_manager.py:129-143` | 小 | 内存泄漏 | **✅ 已修复**（evict 时 pop lock） |
| 32 | SessionHistory graphize 竞态（bool 无锁） | L3 | `session_history.py:264,278` | 小 | 重复图谱化 | **✅ 已修复**（asyncio.Lock） |
| 33 | MemoryGraphizer 多余 LLMService 实例 | L1 | `memory_graphizer.py:60` | 小 | 资源浪费 | |
| 34 | 图谱化失败→ContextWindow 不释放 | L3 | `instance_manager.py:430` | 小 | OOM 风险 | **✅ 已修复**（失败时强制释放） |
| 35 | Snapshot 覆盖写丢数据 + 无原子性 | L3 | `snapshot.py` | 中 | 跨轮数据丢失 | **✅ 覆盖写已修复**（merge_snapshots 读-合并-写；并发锁暂缓） |
| 36 | WorldInstance 伪 async | L3 | `world_instance.py:49` | 中 | 启动慢 | **✅ 已修复**（to_thread 真并行） |
| 37 | 其他异常吞咽（scene_bus/player_node） | L3 | 多处 | 小 | 调试困难 | **✅ 已修复**（8 文件补 logger） |
| 38 | ENGINE_TOOL_EXCLUSIONS 只覆盖 2 个意图 | L1 | `gm_extra_tools.py:27` | 小 | 工具重复执行 | |
| 39 | 队友工具事件类型被覆盖 | L1 | `teammate_response_service.py:1022` | 小 | 信息丢失 | |
| 40 | MCPClientPool 裸异常归一化 | L2 | `mcp_client_pool.py:316,362,754` | 小 | 根因被压缩 | |
| 41 | `app/rules.py` 与 `combat/rules.py` 99% 重复 | L3 | `app/rules.py` vs `combat/rules.py` | **1 行** | 维护混乱 | **✅ 已删除**（合并入 combat/rules） |
| 42 | **combat_mcp_server 实例分叉** | L2 | `combat/combat_mcp_server.py:53-55` | 中 | CombatEngine/SessionStore/EventService 独立实例 | |
| 43 | **event_llm_service 层级错位** | L1 | `admin/event_llm_service.py` | 中 | 纯 LLM 转换逻辑错放 admin/ 目录 | |
| 44 | **report_flash_evaluation raw dict write** | L1 | `immersive_tools.py:354` | 小 | `ctx.session.flash_results[prompt]=` 绕过 facade/脏标记 | |
| 45 | **AgenticExecutor SSE 事件静默丢失** | L1 | `agentic_executor.py:189` | 小 | `except Exception: pass` 吞掉队列错误 | |
| 46 | **AgenticExecutor 硬编码 30s 超时** | L1 | `agentic_executor.py:131` | 小 | 与 settings 分叉 | |
| 47 | **双重 genai.Client 实例** | L1 | `llm_service.py:46` + `image_generation_service.py:22` | 小 | 速率限制不统一 | |
| 48 | **LLMService.generate_simple 异常重包装** | L1 | `llm_service.py:705` | 小 | 原始异常类型丢失 | |
| 49 | **TieredAI _respond_chat 忽略 npc_profile** | L1 | `tiered_ai_service.py:157-163` | 小 | 高 tier 获差上下文 | |
| 50 | **队友决策/响应用不同模型常量** | L1 | `teammate_response_service.py:547` vs `:1099` | 小 | 行为分叉风险 | |
| 51 | **MemoryGraphizer 虚假 Firestore 文档** | L1 | `memory_graphizer.py:42` | 小 | docstring 声称持久化 Firestore 但实际零写入 | |
| 52 | **LLM 入口无统一追踪** | L1 | 多文件 | 中 | 成本不可见 | |

---

## 架构级债务（需要专项建设）

| # | 债务 | 影响层 | 状态 | 说明 |
|---|------|--------|------|------|
| A | **统一 World API 不存在** | L2 | 完全缺失 | 4 套接口各自为战 |
| B | **权限系统不存在** | L2 | 完全缺失 | 任何 Agent 可调任何工具 |
| C | **30 处工具直通 L3** | L1→L3 | 系统性违规 | immersive 18 处 + gm_extra 12 处；需建 L2 后逐个改 |
| D | **4 套接口碎片化** | L2 | 历史遗留 | REST/MCP/Immersive/Extra |
| E | **FlashCPU 是 L1 放在 L2** | L2 | 层级错位 | 需拆分 |
| F | **4 个 L1 文件放在 app/world/** | L3 目录 | 层级错位 | agentic_executor/immersive_tools/gm_extra_tools/role_registry |
| G | **MCP 工具层自建独立实例** | L2 | 缓存分叉 | party/passerby/narrative/inventory/npc/combat 各建独立实例 |
| H | **IntentExecutor 操作顺序倒置（历史）** | L3 | 已修复 | MOVE/USE_ITEM 顺序问题已在 M5 收口，保留审计记录 |
| I | **队友战斗工具重名冲突（历史）** | L1 | 已修复 | `choose_battle_action` 同名冲突已清理，保留审计记录 |
| J | **L1 裸异常洪水（43 处）** | L1 | 可观测性灾难 | 13 处 print + 静默降级/丢弃/全轮失败 |
| K | **L3 反向依赖 L1** | L3→L1 | 架构逆流 | `app/world/npc/instance_manager.py` 直引 `app/services/memory_graphizer.py` |
| L | **Runtime 私有接口泄漏到 L3** | L3→Runtime | 脆弱耦合 | `IntentExecutor` 直调 `_sync_tick_to_narrative` / `_apply_tick_side_effects` |
| M | **L3 混合层残留旧基础模块依赖** | L3/Runtime | 重构尾差 | `session_runtime/event_machine/player/*` 仍挂 `area_runtime/companion_*/CharacterStore` |

---

## 修复路线建议

### 第一波：止血（1-2 天）— L3 部分已完成

1. 🩸 修 `settings.admin_agentic_thinking` → `admin_flash_thinking_level`（1 行）
2. ✅ ~~InstanceManager key 加入 `session_id`~~ — 已完成
3. 🩸 MCP 工具统一走 Coordinator 共享实例（消除实例分叉）
4. ✅ ~~IntentExecutor MOVE/USE_ITEM 操作顺序~~ — 已完成
5. ✅ ~~IntentExecutor 13 处裸异常~~ — 已完成
6. 🩸 PipelineOrchestrator + GmExtraTools 20 处裸异常 → 规范化
7. 🩸 SessionRuntime 恢复补 Firestore 回读（挂起，等重建）
8. 🩸 队友 `choose_battle_action` 工具去重（保留单一战斗入口）
9. 🩸 GmExtraTools P6 block 改用公共方法（消除私有方法调用）

### 第二波：异步化 + 可观测性（3-5 天）— L3 部分已完成

10. ✅ ~~同步 Firestore I/O~~ — M3 删 8 + M4 to_thread 11 + M5 删 31 = 50/50 全消除
11. 🔧 LLMService 7 处同步调用 → `self.client.aio.models`
12. ✅ ~~WorldInstance 真异步化~~ — 已完成（to_thread 包装）
13. 🔧 LLMService 13 处 `print()` → `logger.error()`
14. 🔧 TieredAIService/TeammateResponseService 静默降级/失败补日志

### 第三波：锁与一致性（2-3 天）— L3 部分已完成

15. 🩸 PartyService 加 `asyncio.Lock` + persist-first 模式（挂起，Store 已删）
16. ✅ ~~WorldGraph 脏标记完整化~~ — 已完成
17. ✅ ~~SessionHistory LRU 淘汰~~ — 已完成
18. ✅ ~~GameRuntime per-world 锁~~ — 已完成
19. 🔧 MemoryGraphizer 图谱化加回滚/事务语义

### 第四波：L2 建设（专项）

20. 🏗️ 创建 `WorldAPI` 统一接口
21. 🏗️ 收口所有工具调用走 WorldAPI（30 处 L3 直通）
22. 🏗️ 建立权限矩阵
23. 🏗️ 统一错误响应格式（脱敏）

---

## 数字汇总

| 指标 | 数量 | 当前状态（M6） |
|------|------|---------|
| 同步 I/O 阻塞点（Firestore） | ~~50 处~~ | **✅ 全部消除** |
| 同步 I/O 阻塞点（LLM） | ~~7 处~~ | **✅ 已修复**（sync→async） |
| 裸 `except Exception` | L1: **43 处** + L2: Pipeline 11 + ~~L3: IntentExecutor 13~~ + 其他 | L3 部分已修 |
| API 异常泄露点 | **25+ 处** | 未修 |
| L1 直通 L3 调用点 | **30 处**（immersive 18 + gm_extra 12，含 3 处私有方法） | 未修 |
| NPC/队友 STUB 工具 | **9 个** / 38 总工具 = **24%** | 未修 |
| 无生命周期缓存 | ~~SessionHistory~~ + AdminCoordinator×2 + PasserbyService + NarrativeService | SessionHistory ✅ 已修 |
| LLM 调用点总计 | **20 处**（以 async/委托为主） | 统一追踪未修 |
| genai.Client 实例 | **2 个**（LLMService + ImageGenerationService） | 未修 |
| 队友战斗工具重名冲突 | ~~1 处~~（`choose_battle_action`） | **✅ 已修复** |
| MCP 独立实例分叉 | **6 个**（party/passerby/narrative/inventory/npc_tools/combat_mcp） | 未修 |
| 操作顺序倒置 | ~~2 处~~ | **✅ 全部修复** |
| L1 纯代码模块 | **10 个**（+ 8 个 Prompt 模板） | |
| L2 层模块总数 | **22 个** | |
| L3 纯机械模块 | **47 个**（原 57，M5 删 9 + 合并 1） | |
| 层级错位文件 | **5 个**（4 个 L1 在 world/ + 1 个 L1 在 admin/） | |
| 全部屎点总数（P0-P2 + M6增量） | **61 个** + 13 项架构债 | L3 分离已完成；新增 9 项实现债务待收口 |
