# 待办清单（2026-03-02 更新）

> 测试基线：**898 passed**（2026-03-02 D-S-AOE 完成后；法术 DC + 豁免检定 + 多目标 AOE）

---

## 当前工作：设计文档逐层对照审查

**目标**：将各层实现与 `app/博德之门3架构设计规范/` 设计文档对照，消除屎山、补注释、统一命名。

| 层级 | 状态 | 说明 |
|------|------|------|
| ❶ 内容层（10 registries） | ✅ **已完成** | 屎山清理 + 命名统一 + 推迟项 TODO 注释 |
| ❸ 状态层（10 slices） | ✅ **已完成** | D-S06 Bug+API + D-S07 EventSlice 白名单+trigger()；3 项推迟至上层（见下） |
| ❷ 规则引擎层（13 handlers） | ✅ **已完成** | D-R29~D-R31 屎山清理 + 设计规范 §3.1~§3.13 完整性对照（见下） |
| ❹ 编排层（11 hooks + pipeline） | ✅ **已完成** | O-D01~D03 屎山清理 + O-G01 TimeAdvanceHook 日换商店刷新（轮次1+2）；O-G02 RelationshipHook（轮次3） |
| ❺ 叙事层（AgenticExecutor + tools） | ✅ **已完成** | Bug×2 + 代码气味×2 修复；N-8 确认完成（RoleStateProxy）；合理推迟×3 注明 |

**❶ 内容层完成的具体动作**（2026-03-02）：
- 屎山清理：`MonsterTemplate` 三 gold 字段 → 单 `gold_drop`（load 时别名合并）；`ItemTemplate` heal/price 冗余字段消除；`resolve_item_heal_amount()` 抽到 `handler_utils`
- 命名统一：`FactionTemplate.relations` → `faction_relations`（设计规范名），load 时接受旧名 `relations` 作别名，`world_knowledge_graph.py` 消费端同步
- 推迟项 TODO 注释（6 处）：Connection typed / SubLocationTemplate typed / WeaponData·ArmorData·ConsumableData / SkillEffect·SkillCost typed + StatusEffectTemplate / MonsterTemplate AI 字段 / CharacterTemplate 战斗属性 / LoreEntry scope

---

**❸ 状态层完成的具体动作**（2026-03-02，D-S06 + D-S07）：
- Bug：`RelationSlice.reduce_stock()` 缺 `_dirty = True` 修复
- Read API 补齐：`FlagSlice.get_all()`、`QuestSlice.get_active_quests/get_completion`、`PartySlice.count_critical_moments/get_shared_experiences(with_character)`
- 代码气味：`StateContainer.create_new()` 中 `restore(snapshot())` → `clear_dirty()`
- 设计文档同步：3 处命名分歧（`delete→remove`、`set_stage→set_relationship_stage`、动态任务 `state→status`）
- `EventSlice._VALID_STATES` 白名单 + validate() 检查 + `trigger()` 便捷方法
- 新增测试 22 个（`tests/test_state_read_apis.py`）

**❷ 规则引擎层完成的具体动作**（2026-03-02，D-R29~D-R31）：
- **D-R29** Bug 修复：`batch_execute` 缺少 `state.apply(result.delta)` → 补上累积语义，后续命令可见前命令状态变更；新增 3 个测试（`tests/test_rules_engine.py`）
- **D-R30** 屎山：`spell_resolver.py` 重复定义 `coerce_int`（语义偏差：`3.5→3` 而非 `3.5→None`）→ 改为从 `handler_utils` import；`spell.py._coerce_int` 无意义包装层 → 删除，2 处调用点内联
- **D-R31** 健壮性：9 处 `assert` 在 validate/compute 路径 → 替换为正确返回路径（`ValidationResult(ok=False,...)` / `ExecuteResult.error(...)`）；涉及 `combat.py`（7 处）、`status_effect.py`（1 处）、`spell.py`（1 处）
- **完整性对照**：设计规范 §3.1~§3.13 共 56 个 command_type 全部实现，零缺漏；5 个 D-R 扩展命令（stand_up/consume_resource/investigate/interact_object/tick_combat_effects）全部有施工记录

**❸ 状态层推迟至上层的 3 项（复查结论）**：

| 项目 | 推迟至 | 复查结论 |
|------|--------|----------|
| `PlayerSlice.add_xp()` 升级阈值 | **❷ 规则层** GrowthHandler | ✅ **已完成**：`GrowthHandler._resolve_available_level()` 已正确读取 `ClassRegistry.xp_curve`，`_compute_add_xp` 计算 `available_level` 并在 metadata 返回 `level_up_available`，`_validate_level_up` 同样使用阈值校验 |
| `EventSlice.spread_rumor()` | **❷ 规则层** WorldStateHandler `create_rumor` 深化 | ⏸ **仍推迟**：谣言系统整体零消费端（无 hook/evaluator 读取 `state.events.rumors`），加 ID 基础设施属于过早抽象，等有消费端时再做 |
| `RelationSlice.check_stage_transition()` | **❹ 编排层** 新 RelationshipHook / NpcScheduleHook | ⏸ **仍推迟**：条件跨切片（需 `RelationSlice` + `PartySlice.count_critical_moments()`），Settlement Hook 是天然归宿 |

**❺ 叙事层完成的具体动作**（2026-03-02，设计文档对照审查）：
- **Bug**：`_extract_scene_keywords` teammate 调用时固定用 `"npc"` 角色过滤 L5 → 加 `role` 参数，`_build_l6`/调用方同步（`build_teammate_context` 传 `"teammate"`）
- **Bug**：`RememberTool`/`RevealSecretTool` Command `source="ai_osiris"` 语义错误 → 改为 `source=character_id`（NPC agent 对话发出，非 Osiris）
- **代码气味**：`ToolResult.noop()` `metadata["status"]="stub"` → `"noop"`
- **代码气味**：`OfferTradeTool.applicable_traits` 类属性 → property（与基类 ABC 风格对齐）
- **N-8 确认完成**：`role_proxy.py` `RoleStateProxy` 已实现状态访问隔离，`build_agent_context()` 已注入，待办清单移除
- **合理推迟**（3项，已确认边界）：`share_memory` 读 impressions 而非 MemoryGraph（Phase 2-3）；`AgentContext.character_id` 在 metadata（功能等价）；Prompt 模板完整化（设计文档有占位）

---

## 未完成待办项

### 功能深化（内容层孤立字段 → 补消费端）

> 背景：内容层 10 个 registry 均有消费端，但以下字段已在 dataclass 中定义、无任何代码读取。
> 每项深化的前提是**先补对应的数据**（从 lorabook v2 提取），再实现消费端逻辑。

| # | 子系统 | 严重度 | 涉及层 | 状态 | 说明 |
|---|--------|--------|--------|------|------|
| F-A | **Item 分类型子结构**：`WeaponData` / `ArmorData` / `ConsumableData` | 中 | ❶❷ | ✅ **消费端完成** | ❶ typed sub-struct 迁移完成（D-C-FA）；❷ equip 类型↔槽位验证 + AC 重算（D-R32）；❷-C CombatHandler 武器伤害骰推迟，等武器数据 |
| ~~F-B~~ | ~~**Skill 效果子结构**：`SkillEffect` / `SkillCost` / `StatusEffectTemplate`~~ | ~~高~~ | ~~❶❷❸~~ | ✅ **已完成** | ❶ 三 typed struct 定义；SkillTemplate.effect/cost 迁移；旧键别名归一；StatusEffectTemplate sub-table；❷ spell_resolver/spell_effects 消费端适配；❸ 不改（concentration 已有） |
| ~~F-C~~ | ~~**Monster 战斗 AI**：`ai_personality` / `flee_threshold` / `xp_value` / `attacks`（消费端）~~ | ~~中~~ | ~~❷❹~~ | ✅ **已完成** | MonsterAttack 新增 damage_dice/hit_bonus/damage_type；MonsterTemplate 新增 ai_personality/flee_threshold/xp_reward；CombatHandler 怪物回合（反击+逃跑判定+XP分发）；无 attacks 定义→hold；time_cost=0.0 约束下纯规则层实现（D-R32） |
| ~~F-D~~ | ~~**Map 扩展字段**：Connection typed（与 T-2 联动）~~ | ~~中~~ | ~~❶❷~~ | ✅ **已完成** | `Connection` dataclass（target/type/travel_time_minutes/travel_slots）；`NavigationHandler` 连接校验 + travel_slots time_cost；`world_data_loader._load_maps` 直透传；新增 `get_connections`/`get_connection` 查询方法 |
| ~~F-E~~ | ~~**Class 职业特性树**：`level_features`（消费端）+ `class_resources`~~ | ~~中~~ | ~~❷❸~~ | ✅ **已完成** | `ClassTemplate.class_resources_schema`（独立 resource 配置）；`GrowthHandler` 升级时首次解锁/max增量追加；`RestHandler` 已有，无需改动 |

**依赖关系**：F-B 依赖 F-A（消耗品效果需要 ConsumableData）；F-C 依赖 F-B（怪物技能需要 SkillEffect）；F-D 独立；F-E 部分依赖 F-B（技能关联特性）。

**数据管线前置条件**：实施每项前需先从 lorabook v2 提取对应格式数据（当前消费端已明确，所需字段见上方"运行时消费端"调查结论）。

---

### 战斗系统

> 背景：2026-03-02 行动经济调查结论。设计文档 §3.1 攻击流程已定义行动阻止检查，但代码从未接入。

| # | 待办 | 严重度 | 备注 |
|---|------|--------|------|
| C-1 | `is_action_prevented()` 接入 validate | 高 | **真 Bug**：眩晕/麻痹等状态效果当前形同虚设。`CombatHandler.validate` + `SpellHandler._validate_cast_spell` 各加 1 行检查。成本极低，收益高 |
| C-2 | `defending`/`dashed`/`disengaged` 标志轮末重置 | 中 | **设计残缺**：`_compute_flag_command` 设置后永不清除。前置确认：`player_flags.defending` 是否有消费端（若无消费端则 AC 加成未生效，实际无害）。确认后在 `tick_combat_effects` 补重置 |
| C-3 | `action_used` 完整行动经济（每轮行动限制） | 低 | **功能缺失但无触发场景**：设计文档 §3.1 仅 1 处提及 `action_used = true`，PlayerSlice schema 未定义此字段，设计本身不完整。推迟至战斗平衡阶段。届时需：PlayerSlice 加 `used_actions: dict[str,int]`，CombatHandler + SpellHandler validate 检查，`tick_combat_effects` 重置 |

### 编排层

| # | 待办 | 严重度 | 备注 |
|---|------|--------|------|
| O-1 | AdminCoordinator 抽象引入 | 低 | 等 3+ coordinator 类型时再做（D-O01） |
| ~~O-2~~ | ~~RelationshipHook（`check_stage_transition`）~~ | ~~低~~ | ✅ **已完成**（编排层审查轮次 3，854 passed） |

### 世界状态

| # | 待办 | 严重度 | 备注 |
|---|------|--------|------|
| W-1 | `EventSlice.spread_rumor()` + rumor id 基础设施 | 低 | 谣言系统零消费端（无 hook/evaluator 读取 `state.events.rumors`），等消费端出现时补 id 字段 |

### 测试与数据

| # | 待办 | 严重度 | 备注 |
|---|------|--------|------|
| ~~T-2~~ | ~~数据管线：world_data_loader.py 适配修复~~ | ~~中~~ | ✅ **已完成** — `_load_maps` 直透传（MapRegistry 解析）；`_load_items` properties 展平到顶层；与 F-D 联动完成 |
