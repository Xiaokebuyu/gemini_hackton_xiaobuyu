# 表现层设计 — 视觉小说 (Galgame) 风格

创建时间：2026-02-26
状态：立项，后端先行适配
前置文档：`编排层设计规范.md`（SSE 事件来源），`叙事层设计规范.md`（Agent 叙述输出），`状态层设计规范.md`（SceneSlice 场景数据），`NPC与队友子系统设计规范.md`（四维好感模糊描述）

> 对应 BG3 六层架构的 ❻ 表现层。目标：让文字 CRPG 具备 Galgame 级视觉体验——场景背景、角色立绘、转场动画、粒子氛围。

---

## 一、视觉目标

```
┌─────────────────────────────────────────────────┐
│  [背景图：酒馆内部，暖色调，壁炉火光]              │
│                                                   │
│     [立绘：女神官]          [立绘：哥布林杀手]      │
│      (left, worried)        (right, neutral)      │
│                                                   │
│  ┌─────────────────────────────────────────────┐ │
│  │  女神官                                      │ │
│  │  "那个村庄的人们...我们必须尽快赶过去。"       │ │
│  │                        ▼ 打字机效果           │ │
│  │                                              │ │
│  │  [🎲 说服 DC12] 安抚她   [直接出发]   [再等等] │ │
│  └─────────────────────────────────────────────┘ │
│  HP ████████░░ 42/60    ⏰ 14:00 午后    💰 230G  │
└─────────────────────────────────────────────────┘
```

核心视觉元素：
- **场景背景** — 每个地点一张（可选：时段变体 morning/afternoon/evening/night）
- **角色立绘** — 全身立绘 + 表情差分（neutral/happy/angry/sad/surprised）
- **对话框** — 底部半透明面板，打字机效果，角色名高亮
- **选项按钮** — 支持能力检定标签 `[说服 DC14]`
- **转场动画** — 场景切换时 fade/dissolve/slide
- **粒子氛围** — 雨/雪/火星/魔法光点/灰尘（按地点 ambiance 标签）
- **HUD** — HP 条、时间槽、金币，常驻底栏或角落

---

## 二、前端技术栈（推荐）

| 层 | 技术 | 职责 |
|---|---|---|
| 视觉渲染 | **PixiJS 8** | 背景图、立绘、转场、粒子特效、光影 |
| 游戏 UI | **React 19** | 对话框、选项、背包、角色面板、地图 |
| 桥接 | **@pixi/react** | PixiJS 场景嵌入 React 组件树 |
| 状态管理 | **Zustand** | 轻量，适合游戏状态 |
| 通信 | **EventSource (SSE)** | 接后端流式事件 |
| 构建 | **Vite** | 快速 HMR |

不选的方案及原因：
- Phaser — 完整游戏引擎太重，VN 不需要物理/碰撞
- Three.js — 3D 引擎，文字游戏不需要
- 纯 CSS — 粒子/转场效果受限，无法做复杂视觉
- Monogatari.js — VN 专用但封闭，扩展不了 RPG 系统（背包/战斗/地图）

---

## 三、SSE 视觉事件协议

这是前后端的"合同"。后端 SSE 事件需要携带视觉指令，前端据此驱动渲染。

### 3.1 现有事件扩展

```jsonc
// npc_response — 扩展 character 视觉信息 + 情感
{
  "event": "npc_response",
  "data": {
    "character_id": "priestess",
    "character_name": "女神官",
    "text": "那个村庄的人们...我们必须尽快赶过去。",
    "emotion": "worried",        // ← FEELING_MAP 情感维度映射
    "portrait": "priestess/worried.png",  // 表情立绘 key
    "is_streaming": true         // 是否流式输出（打字机效果）
  }
}

// gm_narration — 环境叙述（无立绘，可能触发氛围变化）
{
  "event": "gm_narration",
  "data": {
    "text": "壁炉的火光在墙壁上投下摇曳的影子...",
    "ambiance_hint": "firelight"  // 可选：前端据此加粒子
  }
}

// gm_comment — GM 毒舌点评（不同渲染风格：斜体/灰色/旁白框）
{
  "event": "gm_comment",
  "data": {
    "text": "又在酒馆坐了整整一天，哥布林表示感谢。",
    "tone": "sarcastic"          // sarcastic | dry | impressed | ominous
  }
}

// dialogue_options — 扩展检定标签
{
  "event": "dialogue_options",
  "data": {
    "options": [
      {"id": 1, "text": "安抚她", "check": {"type": "persuasion", "dc": 12}},
      {"id": 2, "text": "直接出发", "check": null},
      {"id": 3, "text": "再等等", "check": null}
    ]
  }
}
```

### 3.2 新增视觉事件

```jsonc
// scene_change — 场景切换（进入新地点/子地点时触发）
{
  "event": "scene_change",
  "data": {
    "location_id": "tavern_ground_floor",
    "location_name": "酒馆一楼",
    "background": "locations/tavern_ground_floor/afternoon.png",
    "transition": "fade",         // fade | dissolve | slide_left | slide_right | cut
    "duration_ms": 800,
    "ambiance": "tavern_warm",    // 粒子预设 key
    "lighting": "warm_indoor"     // 色调预设 key
  }
}

// character_enter — 立绘入场
{
  "event": "character_enter",
  "data": {
    "character_id": "priestess",
    "character_name": "女神官",
    "sprite": "characters/priestess/full_body.png",
    "position": "left",           // left | center | right
    "emotion": "neutral",
    "animation": "fade_in",       // fade_in | slide_in_left | slide_in_right
    "duration_ms": 400
  }
}

// character_exit — 立绘退场
{
  "event": "character_exit",
  "data": {
    "character_id": "priestess",
    "animation": "fade_out",
    "duration_ms": 300
  }
}

// character_emotion — 表情变化（不重新入场，只换表情）
{
  "event": "character_emotion",
  "data": {
    "character_id": "priestess",
    "emotion": "happy",
    "portrait": "priestess/happy.png"
  }
}

// dice_roll — 骰子动画
{
  "event": "dice_roll",
  "data": {
    "type": "d20",
    "result": 15,
    "modifier": 3,
    "total": 18,
    "dc": 14,
    "success": true,
    "skill": "persuasion",
    "roller": "player"
  }
}

// status_update — HUD 数值变化（带动画方向）
{
  "event": "status_update",
  "data": {
    "hp": {"current": 42, "max": 60, "delta": -8},    // delta 负=掉血动画 正=治疗
    "gold": {"current": 230, "delta": -50},
    "xp": {"current": 1200, "delta": 100},
    "time": {"slot": "afternoon", "hour": 14, "minute": 0}
  }
}

// vfx — 一次性视觉特效（战斗/魔法/特殊事件）
{
  "event": "vfx",
  "data": {
    "effect": "heal_glow",        // 预设特效 key
    "target": "player",           // player | character_id | "screen"
    "duration_ms": 1500
  }
}
```

### 3.3 氛围预设表

前端维护一套粒子/色调预设，后端只传 key：

| ambiance key | 粒子效果 | 说明 |
|---|---|---|
| `tavern_warm` | 零星火星上升 + 灰尘飘浮 | 室内酒馆 |
| `forest_calm` | 树叶飘落 + 光斑 | 平静森林 |
| `forest_dark` | 萤火虫 + 薄雾 | 夜晚/阴暗森林 |
| `rain_light` | 细雨 | 小雨 |
| `rain_heavy` | 暴雨 + 闪电（偶发白屏闪） | 暴风雨 |
| `snow` | 雪花飘落 | 雪天 |
| `cave_damp` | 水滴 + 薄雾 | 洞穴 |
| `town_busy` | 灰尘 + 远处烟囱烟雾 | 热闹城镇 |
| `battle_tense` | 火星 + 暗红色调 | 战斗氛围 |
| `magic_arcane` | 蓝紫色光点环绕 | 魔法场景 |
| `none` | 无 | 清洁场景 |

| lighting key | 色调效果 |
|---|---|
| `warm_indoor` | 暖黄 overlay, 柔和阴影 |
| `cool_outdoor` | 自然光, 无调色 |
| `night` | 深蓝 overlay, 低亮度 |
| `sunset` | 橙红渐变 |
| `dungeon` | 暗绿/灰, 高对比 |
| `magical` | 淡紫 overlay, 微光 |

---

## 四、后端适配清单

后端需要改的东西，按优先级排：

### P0：数据模型加 visual 字段

**地点模型** — 每个 location 需要关联背景图和氛围：

```python
# 在地点数据中追加（WorldInstance 加载时带入）
"visual": {
    "background": "locations/tavern_ground_floor.png",  # 资产 key
    "ambiance": "tavern_warm",
    "lighting": "warm_indoor",
    "time_variants": {                                   # 可选
        "night": "locations/tavern_ground_floor_night.png"
    }
}
```

**角色模型** — NPC/队友需要视觉资产：

```python
# 角色数据中追加
"visual": {
    "sprite": "characters/priestess/full_body.png",
    "portrait": "characters/priestess/portrait.png",
    "expressions": {
        "neutral": "characters/priestess/neutral.png",
        "happy": "characters/priestess/happy.png",
        "angry": "characters/priestess/angry.png",
        "sad": "characters/priestess/sad.png",
        "surprised": "characters/priestess/surprised.png"
    }
}
```

### P1：SSE 事件扩展

- `PipelineOrchestrator` — 导航时发 `scene_change` 事件
- `NpcInteractionCoordinator` — NPC 回复时带 `emotion` + `portrait`
- `AgenticExecutor` — 工具调用结果映射到 `status_update`（heal→hp delta, damage→hp delta）
- `TeammateResponseService` — 队友发言带立绘信息

### P2：AssetResolver — 资产解析模块

机械模块，负责将资产 key 解析为可用 URL。不含 LLM——prompt 构造是模板化的。

**三级解析链**：

```
SSE 事件需要资产（如 scene_change.background）
    │
    ├─ L1: 缓存命中？→ 返回 URL（内存 / Redis）
    │
    ├─ L2: 静态资产表命中？→ 返回 URL
    │       资产表来源：❶ WorldInstance 加载时带入的 visual 字段
    │       格式：{ asset_key: url }
    │       覆盖：预制角色立绘、手绘场景、表情差分
    │
    └─ L3: 动态生成 → 构造 prompt → 调生图 API → 上传 Cloud Storage → 缓存 → 返回 URL
            触发场景：临时子区域（DynamicSubAreaManager）、临时路人 NPC（PasserbyService）、
                      缺失的时段变体、从未访问的地点首次进入
```

**Prompt 构造（模板化，非 LLM）**：

```python
class AssetPromptBuilder:
    """从结构化数据拼接生图 prompt。不调用 LLM。"""

    def build_scene_prompt(self, location_data: dict) -> str:
        """从地点数据构造场景生图 prompt。

        输入：description + tags + terrain_type + lighting + ambiance
        输出：结构化 prompt 字符串
        """
        # 示例输出：
        # "fantasy RPG scene, interior of a medieval tavern, warm firelight,
        #  wooden beams, stone walls, busy atmosphere, afternoon lighting,
        #  visual novel style, detailed background, no characters"

    def build_portrait_prompt(self, character_data: dict, emotion: str) -> str:
        """从角色数据构造立绘生图 prompt。

        输入：appearance + personality + tags + emotion
        输出：结构化 prompt 字符串
        """
        # 示例输出：
        # "fantasy RPG character portrait, middle-aged male farmer,
        #  torn clothes, bandaged left arm, fearful expression,
        #  visual novel style, full body, transparent background"
```

**API**：

```python
class AssetResolver:
    """资产解析器。机械模块，非 Agent。"""

    async def resolve(self, asset_key: str, context: dict | None = None) -> str:
        """解析资产 key 为 URL。context 用于 L3 动态生成时提供描述信息。

        asset_key 格式约定：
          场景：  "locations/{location_id}/{time_variant}.png"
          立绘：  "characters/{character_id}/full_body.png"
          表情：  "characters/{character_id}/{emotion}.png"
          临时：  "temp/{sub_area_id}/background.png"
                  "temp/{npc_id}/portrait.png"
        """

    async def resolve_batch(self, keys: list[str], contexts: dict[str, dict]) -> dict[str, str]:
        """批量解析。场景切换时可能同时需要背景 + 多个角色立绘。"""

    async def prefetch(self, asset_keys: list[str], contexts: dict[str, dict]) -> None:
        """预取。NarrativePlanner 生成临时子区域时提前调用，
        避免玩家进入时才触发生图（延迟太高）。"""

    def invalidate(self, asset_key: str) -> None:
        """缓存失效。临时子区域过期清理时调用。"""
```

**与 Pipeline 的集成**：

```
NarrativePlanner.plant_environmental()
    → DynamicSubAreaManager.create(sub_area)
    → AssetResolver.prefetch(["temp/{id}/background.png"], context=sub_area)  ← 预取，不阻塞
    ...（若干格后）...
玩家 enter_sub_location
    → AssetResolver.resolve("temp/{id}/background.png")  ← 命中缓存，立即返回
    → SSE: scene_change { background: url }

PasserbyService.spawn(npc)
    → AssetResolver.prefetch(["temp/{npc_id}/portrait.png"], context=npc_profile)
    ...
NPC 出现在对话中
    → AssetResolver.resolve("temp/{npc_id}/portrait.png")
    → SSE: character_enter { sprite: url }
```

**资产来源优先级**：

| 来源 | 质量 | 延迟 | 成本 | 适用 |
|------|------|------|------|------|
| 手工素材（画师/素材包） | 最高 | 0 | 前期投入 | 主要角色、核心场景 |
| AI 生成 + 缓存 | 中等 | 首次 3-10s，后续 0 | 按次计费 | 临时内容、缺失变体 |
| 通用占位图 | 低 | 0 | 0 | 兜底（生图失败/超时） |

**兜底策略**：生图 API 超时或失败时，返回同类型通用占位图（如 `fallback/forest_generic.png`），不阻塞游戏流程。前端收到占位图后正常渲染，后台异步重试生图。

### P3：FEELING_MAP → emotion 映射

已有 `FEELING_MAP`（dimension × level × polarity → disposition delta），需要追加一层：
disposition delta → 前端 emotion key 的映射，让立绘表情和情感系统联动。

```python
# 简单映射规则
EMOTION_FROM_DISPOSITION = {
    "very_positive": "happy",     # 好感大幅上升
    "positive": "neutral",        # 微上升
    "neutral": "neutral",
    "negative": "sad",            # 微下降
    "very_negative": "angry",     # 大幅下降
    "hostile": "angry",
}
```

---

## 五、开发顺序

```
后端 (现在就可以做)              前端 (后续)
────────────────────          ────────────────────
P0: 数据模型加 visual 字段  →   搭脚手架 (Vite+React+PixiJS)
P1: SSE 事件扩展            →   对话框组件 + 打字机效果
P2: AssetResolver 模块       →   PixiJS 场景渲染 (背景+立绘+转场)
    三级解析链 + 预取机制        资产加载 + 占位图处理
P3: emotion 映射             →   粒子系统 + HUD
                                 背包/角色面板 UI
                                 地图组件
```

后端改动不大——核心是在现有数据和 SSE 事件上加字段 + AssetResolver 模块。AssetResolver 是机械模块（非 Agent），模板化 prompt 构造 + 生图 API 调用，不引入额外 LLM 成本。

---

## 变更日志

| 日期 | 变更 |
|---|---|
| 2026-02-26 | 立项。Galgame 风格定义 + 前端技术栈 + SSE 视觉事件协议(9 种事件) + 氛围预设表 + 后端适配清单(P0-P3) + 开发顺序 |
| 2026-02-26 | P2 资产管理 → AssetResolver 模块设计：三级解析链（缓存 → 静态表 → 动态生图）+ 模板化 prompt 构造（非 LLM）+ prefetch 预取机制（NarrativePlanner/PasserbyService 生成临时内容时提前生图）+ 兜底占位图策略 |
