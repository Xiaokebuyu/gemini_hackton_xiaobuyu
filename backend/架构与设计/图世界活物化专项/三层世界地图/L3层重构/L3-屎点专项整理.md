# L3 屎点专项整理 — 代码对齐版

> 更新时间：2026-02-24
> 范围：`app/world/` + `app/runtime/` + `app/services/session_history.py` + `app/services/admin/state_manager.py`
> 口径：只看代码结构与维护成本，不以“当前是否能跑”为唯一标准

---

## 1. L3 体检摘要

| 项 | 数值 |
|----|------|
| L3 文件数（非 `__init__`） | 47 |
| L3 代码总行数 | 17,905 |
| `except Exception` 总数 | 45 |
| >1000 行文件 | 5 |
| L3 最大文件 | `combat_engine.py`（1567） |

### 异常热点（L3）

| 文件 | `except Exception` |
|------|--------------------|
| `app/runtime/session_runtime.py` | 18 |
| `app/runtime/event_machine.py` | 7 |
| `app/world/intent/executor.py` | 6 |
| `app/world/graph/builder.py` | 3 |
| `app/world/graph/snapshot.py` | 3 |
| `app/world/intent/resolver.py` | 3 |

---

## 2. P0 屎点（高优先）

### ~~P0-1：L3 仍依赖已删除 Store 模块~~ ✅ 已完成

> **2026-02-24 已清理**：CharacterStore / PartyStore / GameSessionStore 全部 .pyc 已删除，`app/` 下零 Store import 残留。
> - `character.py` / `ability_check.py`：store=None，import 删除
> - `admin_coordinator.py`：character_store=None
> - `party_tools.py` / `character_tools.py` / `inventory_tools.py`：stub 化
> - `combat_mcp_server.py`：GameSessionStore import 删除 + session_store=None
>
> **已断功能**：`CharacterService`（角色创建/装备/XP）、`AbilityCheckService`（d20检定）、MCP party/character/inventory 工具
> **不受影响**：主管线角色读取已走 `session.player`（PlayerNodeView / WorldGraph）
> **待重写**：角色创建流程、装备变更、能力检定 → 改走 SessionRuntime + WorldGraph

### ~~P0-2：Runtime 仍依赖已删除 `area_runtime`~~ ✅ 已完成

> **2026-02-24 已清理**：session_runtime（4处 import/调用）、event_machine（5处）、context_assembler（2处）、executor（1处）全部删除 + .pyc 缓存删除。纯清理策略，测试 +57 passed。

---

## 3. P1 屎点（结构性）

### ~~P1-1：循环依赖（真实存在）~~ ✅ 已修复

> **2026-02-24 已修复**：`builder.py` 引入 `SessionSnapshot` Protocol 替代对 `SessionRuntime` 的直接 import。循环断掉：builder 不再 import session_runtime，session_runtime 仅 lazy import builder（函数内）。

- ~~`app/world/graph/builder.py:33` 导入 `SessionRuntime`~~ → 改为 `SessionSnapshot` Protocol
- `app/runtime/session_runtime.py:397` 导入 `GraphBuilder`（lazy，函数内，无循环）

### P1-2：超大文件集群（基本完成）

| 文件 | 原行数 | 当前 | 状态 |
|------|--------|------|------|
| `app/world/combat/combat_engine.py` | 1567 | 1567 | 暂不动（战斗系统未开发） |
| `app/world/narrative/narrative_service.py` | 1332 | 966 | ✅ 拆出 `narrative_parser.py`（391行） |
| `app/world/graph/builder.py` | 1254 | 831 | ✅ 拆出 `event_behaviors.py`（462行） |
| `app/world/events/behavior_engine.py` | 1202 | 613 | ✅ 拆出 `condition_evaluator.py`（351行）+ `action_executor.py`（257行） |
| `app/runtime/session_runtime.py` | 1128 | 1057 | 已瘦身（AreaRuntime 清理），暂不继续拆 |

影响：修改一个需求会跨多个职责块，回归面巨大。

### P1-3：门面层过载（SessionRuntime）

- 文件 1128 行，虽然有 `event_machine/memory_ops/narrative_ops/stat_ops` 提取，但仍承担大量生命周期与桥接职责。
- 多处宽异常吞咽使问题定位滞后。

### P1-4：Intent 路径仍“脆”

- `app/world/intent/executor.py` 仍有 6 处宽异常。
- MOVE/USE_ITEM 历史顺序问题已修，但可维护性仍依赖大量分支逻辑和回退逻辑。

---

## 4. P2 屎点（可维护性）

### P2-1：封装边界偏软

- `snapshot` 与 `world_graph` 内部状态耦合仍较深。
- 内部实现细节暴露给跨模块恢复流程。

### P2-2：兼容注释/历史分支较多

- 多个 L3 文件保留“兼容旧路径/临时策略”注释。
- 新人阅读成本高，真实主链不够显式。

### P2-3：测试覆盖不均衡

- 图与记忆链路覆盖较好。
- runtime 深路径（session/event machine）仍偏集成风格，回归定位粒度不足。

---

## 5. L3 分层评分

| 维度 | 评分 | 说明 |
|------|------|------|
| 物理分层 | 8/10 | `world/runtime` 主体已经收拢 |
| 依赖方向 | 7/10 | ~~runtime↔builder 循环~~ ✅ 已断（Protocol）；旧 Store 残留 ✅ 已清 |
| 复杂度控制 | 7/10 | builder(831) + behavior_engine(613) + narrative_service(966) 已达标；>1000 行仅剩 combat_engine(1567 暂不动) + session_runtime(1057 暂不拆) |
| 错误可观测性 | 6/10 | 宽异常偏多，定位速度受限 |
| 综合 | **6.2/10** | 从”混放屎山”转为”高耦合引擎群”，文件拆分基本完成 |

---

## 6. 建议整改路径（L3 专项）

### 第一阶段（止血，1-2 天）

1. 去掉 `character_store` / `area_runtime` 残留导入。
2. 保留向后兼容入口，但在新入口强制走 WorldGraph/SessionRuntime 新主链。

### 第二阶段（结构收口，2-3 天）

1. 打断 `session_runtime ↔ builder` 循环（Protocol/DTO）。
2. `session_runtime` 再拆分一层应用服务，降低“门面过载”。

### 第三阶段（复杂度治理，3-5 天）

1. 拆分 5 个 >1000 行文件（优先 `combat_engine`、`builder`、`narrative_service`）。
2. 将 `except Exception` 替换为可分类异常并补日志语义。

---

## 7. 验收标准

1. ~~L3 不再直接 import 已删除 Store/AreaRuntime。~~ ✅ 全部完成（AreaRuntime + 3 个 Store .pyc 清除）。
2. ~~循环依赖清零（至少清除 runtime↔builder）。~~ ✅ 已修复（SessionSnapshot Protocol）。
3. L3 `except Exception` 从 45 降到 <20。
4. >1000 行 L3 文件从 5 降到 <=2。目前 2 个（combat_engine 1567 暂不动 / session_runtime 1057 暂不拆）。builder(831)、behavior_engine(613)、narrative_service(966) 均已达标。✅ 达标

> 结论：P0 全部完成，P1-1 已修复，P1-2 完成 3/4（builder + behavior_engine + narrative_service），仅剩 session_runtime 暂缓。
