# L1 层重构 — 执行计划

> **创建时间**: 2026-02-24
> **状态**: Phase A ✅ / Phase B ✅ / Phase C 待执行
> **前置**: L2 层延伸重构已完成（Phase A + Phase B，WorldAPI 已建立）
> **测试基线**: 655 passed / 126 failed / 15 errors（零回归目标）
> **Phase A 后测试**: 679 passed / 126 failed / 14 errors（+24 pass，-1 error）
> **Phase B 后测试**: 694 passed / 131 failed / 12 errors（+15 pass，-2 error；5个额外fail来自原collection error转为可运行非回归）
> **参考文档**: `L1-上层Agent地图.md`（深审定稿）、`全量文件清单与层级归类.md`、`架构现状盘点与重构方向.md`

---

## 目标

将散落在 `app/services/` 的 L1 文件收拢到 `app/agentic/`，按子系统组织；清理死代码；溶解 FlashCPU 万能容器。

---

## 当前 L1 居民盘点

### 已归位（无需动）

| 文件 | 位置 | 行数 |
|------|------|------|
| `agentic_executor.py` | `app/agentic/` | 197 |
| `immersive_tools.py` | `app/agentic/` | ~577 |
| `gm_extra_tools.py` | `app/agentic/` | 409 |
| `role_registry.py` | `app/agentic/` | 64 |
| 8 个 prompt 模板 | `app/prompts/` | — |

### 需要处理

| 文件 | 行数 | 位置 | 处置 |
|------|------|------|------|
| `memory_graphizer.py` | 487 | `services/` | Phase A 迁入 `agentic/` |
| `event_llm_service.py` | 270 | `services/` | Phase A 迁入 `agentic/` |
| `tiered_ai_service.py` | 218 | `services/` | Phase A 删除（死代码） |
| `teammate_response_service.py` | 1186 | `services/` | Phase B 迁入 `agentic/teammate/` |
| `flash_cpu_service.py` | 503 | `services/admin/` | Phase C 溶解 |

### 重新定级（不是 L1）

| 文件 | 行数 | 原标记 | 实际 | 处置 |
|------|------|--------|------|------|
| `llm_service.py` | 917 | L1 | **Infra**（Gemini SDK 适配器） | Phase A 标注，不搬 |
| `image_generation_service.py` | 152 | L1 | **Infra**（Gemini Image SDK 适配器） | Phase A 标注，不搬 |

---

## Phase A：L1 清理与简单迁移 ✅

> 低风险快速收益：删死代码 + 搬两个干净文件 + 更新地图文档

### Step A1：删除 `tiered_ai_service.py`（死代码清理）

**背景**：三层 AI 服务（FAST/SUBCONSCIOUS/DEEP），但 Secondary/Main NPC 现在走 AgenticExecutor。唯一调用者 `passerby_service.py` 只走 FAST 路径（`_respond_fast()`）。SUBCONSCIOUS/DEEP 两条路径是死代码。

**操作**：

1. 将 `_respond_fast()` 的 prompt 构建 + LLM 调用逻辑提取为 `app/agentic/passerby_llm.py`（~50 行），保持 L1 逻辑在 L1 层
2. `passerby_service.py`（L2）改为调用 `agentic/passerby_llm.generate_passerby_response()`，不直接持有 LLM 调用
3. 删除 `app/services/tiered_ai_service.py`
4. 清理 `config.py` 中仅被 TieredAIService 使用的 NPC 三层模型配置（如果有的话，需确认）
5. 更新相关导入

> **设计原则**：L2 的 `passerby_service` 只做调度编排，LLM 调用属于 L1 能力，应留在 `app/agentic/` 层。避免内联产生 L1/L2 混合。

**涉及文件**：

| 文件 | 操作 |
|------|------|
| `app/services/tiered_ai_service.py` | **删除** |
| `app/agentic/passerby_llm.py` | **新建**：从 TieredAI 提取 `_respond_fast` 逻辑 |
| `app/services/passerby_service.py` | 改造：调用 `agentic/passerby_llm` 替代 TieredAIService |
| `app/config.py` | 检查是否有仅 TieredAI 使用的配置项 |

### Step A2：迁移 `event_llm_service.py` → `app/agentic/`

**背景**：干净的 L1 文件（270 行），两个 LLM 方法（`parse_event` + `encode_gm_event`），被 `event_service.py`（L2）调用。

**操作**：

1. `mv app/services/event_llm_service.py → app/agentic/event_llm_service.py`
2. 更新 `app/services/admin/event_service.py` 的导入路径（lazy import）
3. 更新 `app/agentic/__init__.py` 导出

**涉及文件**：

| 文件 | 操作 |
|------|------|
| `app/agentic/event_llm_service.py` | **新位置** |
| `app/services/admin/event_service.py` | 更新 lazy import 路径 |
| `app/agentic/__init__.py` | 更新导出 |

### Step A3：迁移 `memory_graphizer.py` → `app/agentic/`

**背景**：L1 文件（487 行），LLM 对话→图谱转换。L3 写入部分已在 L2 重构时提取到 `app/world/memory/graph_writer.py`。

**操作**：

1. `mv app/services/memory_graphizer.py → app/agentic/memory_graphizer.py`
2. 更新所有导入方（完整列表，Codex 审计补全）：
   - `app/world/npc/instance_manager.py`（lazy import）
   - `app/runtime/memory_ops.py`（lazy import）
   - `app/services/admin/pipeline_orchestrator.py:1274`（lazy import）
   - `app/services/__init__.py:10`（模块导出）
   - `app/services/session_history.py:24`（TYPE_CHECKING import）
   - 测试文件
3. 更新 `app/agentic/__init__.py` 导出

**涉及文件**：

| 文件 | 操作 |
|------|------|
| `app/agentic/memory_graphizer.py` | **新位置** |
| `app/world/npc/instance_manager.py` | 更新 lazy import |
| `app/runtime/memory_ops.py` | 更新 lazy import |
| `app/services/admin/pipeline_orchestrator.py` | 更新 lazy import（:1274） |
| `app/services/__init__.py` | 更新模块导出（:10） |
| `app/services/session_history.py` | 更新 TYPE_CHECKING import（:24） |
| 测试文件 | 更新 import |

### Step A4：Infra 重新定级标注

**背景**：`llm_service.py` 和 `image_generation_service.py` 实际是 Gemini SDK 适配器（Infra），不是 L1 居民。物理位置暂不搬动（避免大范围 import 变更），仅在地图文档中标注。

**操作**：

1. 更新 `全量文件清单与层级归类.md`：`llm_service.py` 和 `image_generation_service.py` 标记为 **Infra**
2. 更新 `L1-上层Agent地图.md`：将这两个文件从 L1 列表移到 Infra 备注

### Step A5：全量验证

```bash
python3 -m pytest tests/ -q --tb=short --continue-on-collection-errors
```

预期：655 passed / 126 failed / 15 errors（零回归）

### 执行记录（2026-02-24）

**实际结果：679 passed / 126 failed / 14 errors（+24 pass，-1 error，零回归）**

#### A1 实际执行

| 项目 | 状态 | 说明 |
|------|------|------|
| 新建 `app/agentic/passerby_llm.py` | ✅ | 提取 `_build_fast_prompt()` + `generate_passerby_response()`，约 50 行 |
| 改造 `app/services/passerby_service.py` | ✅ | 移除 `TieredAIService`/`AITier` import；增加缺失的 `logger` 定义（修复潜在 NameError bug）；`handle_passerby_dialogue` 改调 `generate_passerby_response()`；返回值从 `.content` 改为 `["content"]` |
| 删除 `app/services/tiered_ai_service.py` | ✅ | 218 行死代码清除 |
| `config.py` 检查 | ✅ | 确认无仅被 TieredAI 使用的配置项，无需改动 |

#### A2 实际执行

| 项目 | 状态 | 说明 |
|------|------|------|
| 移动到 `app/agentic/event_llm_service.py` | ✅ | |
| 更新 `app/services/admin/event_service.py:44` | ✅ | lazy import 改为 `from app.agentic.event_llm_service import EventLLMService` |
| **额外修复：懒加载 LLMService** | ✅ | 文件顶层 `from app.services.llm_service import LLMService` 改为 `__init__` 内懒加载，避免触发 `app.services.__init__` → `mcp` 链式导入（测试环境无 mcp 包） |
| `app/agentic/__init__.py` 导出 | ❌ | 未更新（文件仅含 docstring，无 `__all__`，不影响功能） |

#### A3 实际执行

| 项目 | 状态 | 说明 |
|------|------|------|
| 移动到 `app/agentic/memory_graphizer.py` | ✅ | |
| `app/runtime/memory_ops.py:109` | ✅ | lazy import 更新 |
| `app/services/session_history.py:24` | ✅ | TYPE_CHECKING import 更新 |
| `app/services/__init__.py:10` | ✅ | 模块导出路径更新 |
| `tests/test_memory_graphizer_wg.py:57` | ✅ | |
| `app/services/admin/pipeline_orchestrator.py:1274` | ✅ | 执行后发现遗漏，补修 lazy import |
| **⚠️ `app/world/npc/instance_manager.py:403`** | ❌ **遗漏** | 仍使用旧路径 `from app.services.memory_graphizer import MemoryGraphizer`。对应 `.py` 已删除、`.pyc` 无法独立加载，**NPC 实例图谱化代码路径运行时会报错**。已列入待修复项。 |
| `app/agentic/__init__.py` 导出 | ❌ | 同 A2，未更新（不影响功能） |

#### A4 实际执行

| 项目 | 状态 | 说明 |
|------|------|------|
| `全量文件清单与层级归类.md` 标注 Infra | ❌ | 未执行 |
| `L1-上层Agent地图.md` 标注 Infra | ❌ | 未执行 |

> **待补：** `instance_manager.py:403` 遗漏的 import 更新需在 Phase C 前修复（或作为独立补丁）。

---

## Phase B：队友服务迁移 + agentic/ 子系统组织 ✅

> 中等风险：搬大文件 + 建立子目录结构

### Step B1：创建 `app/agentic/teammate/` 子目录

**操作**：

1. 创建 `app/agentic/teammate/__init__.py`
2. `mv app/services/teammate_response_service.py → app/agentic/teammate/response_service.py`
3. 更新所有导入方：
   - `app/services/admin/pipeline_orchestrator.py`
   - `app/services/admin/admin_coordinator.py`
   - `app/services/__init__.py`（如果有导出）
   - 测试文件
4. 顶层辅助函数 `_is_combat_active()` 和 `_make_combat_action_tool()` 随文件一起搬（它们是 teammate 专属）

**涉及文件**：

| 文件 | 操作 |
|------|------|
| `app/agentic/teammate/__init__.py` | **新建** |
| `app/agentic/teammate/response_service.py` | **新位置**（原 `services/teammate_response_service.py`） |
| `app/services/admin/pipeline_orchestrator.py` | 更新 import |
| `app/services/admin/admin_coordinator.py` | 更新 import |
| `app/services/__init__.py` | 移除旧导出（如有） |
| 测试文件 | 更新 import |

### Step B2（原 B1~B3 合并执行）：移动文件 + 更新导入

实际执行分为三步：B1 创建目录、B2 移动文件、B3 更新导入（对应原计划结构）。

### Step B3（原）：验证 + agentic/ 结构确认

验证后，`app/agentic/` 最终结构：

```
app/agentic/
├── __init__.py
├── agentic_executor.py          # Agent 执行循环（GM/NPC/队友共用）
├── immersive_tools.py           # 30 个沉浸式工具 + AgenticContext + bind
├── gm_extra_tools.py            # 8 个 GM MCP 依赖工具
├── role_registry.py             # 角色→工具集映射
├── memory_graphizer.py          # 对话→图谱 LLM 转换（Phase A 迁入）
├── event_llm_service.py         # 事件 NLP 3 步管线（Phase A 迁入）
└── teammate/                    # 队友 AI 子系统（Phase B）
    ├── __init__.py
    └── response_service.py      # 队友决策 + Agentic 响应（1186 行）
```

### Step B4（原 B3）：全量验证

```bash
python3 -m pytest tests/ -q --tb=short --continue-on-collection-errors
```

预期：655 passed（零回归）

### 执行记录（2026-02-24）

**实际结果：694 passed / 131 failed / 12 errors（Phase B 后，相对 Phase A +15 pass，-2 error）**

> 131 failed 中有 5 个来自原 Phase A collection error（`test_teammate_migration.py` 之前因 import 链失败无法收集）转为可运行后的测试失败，并非回归。20 个 teammate 相关测试全部通过。

#### B1 实际执行

| 项目 | 状态 | 说明 |
|------|------|------|
| 创建 `app/agentic/teammate/__init__.py` | ✅ | 内容：`"""L1 Teammate AI subsystem — decision making and agentic response generation."""` |

#### B2 实际执行

| 项目 | 状态 | 说明 |
|------|------|------|
| 移动到 `app/agentic/teammate/response_service.py` | ✅ | 原 `app/services/teammate_response_service.py`，1186 行 |
| **额外修复：懒加载 LLMService** | ✅ | 同 A2，`__init__` 参数类型从 `Optional[LLMService]` 改为 `Optional[Any]`，`__init__` 内懒加载，顶层 import 删除，避免 `app.services.__init__` 链式导入 |

#### B3 实际执行（导入方更新）

| 文件 | 状态 | 旧路径 → 新路径 |
|------|------|------|
| `app/services/admin/admin_coordinator.py:45` | ✅ | `app.services.teammate_response_service` → `app.agentic.teammate.response_service` |
| `tests/test_teammate_migration.py:42` | ✅ | 同上（顶层 import：`_is_combat_active`, `_make_combat_action_tool`） |
| `tests/test_teammate_migration.py:125` | ✅ | 同上（方法体内 lazy import：`TeammateResponseService`） |
| `tests/test_teammate_response_service.py:53` | ✅ | 同上（顶层 import：`TeammateResponseService`） |
| `pipeline_orchestrator.py` | ✅ 无需改 | 通过构造参数注入，无直接 import |
| `app/services/__init__.py` | ✅ 无需改 | `TeammateResponseService` 从未在 `__all__` 中导出 |

#### B3 额外修复：测试 stub 补全

`test_teammate_migration.py` 中 `_install_mcp_stubs()` 新增 3 个已删除服务模块的 stub，使 `TestRunAgenticGenerationPayload` 的 5 个测试能在调用 `_run_agentic_generation_payload`（内部有 `from app.services.world_api import WorldAPI` 触发 `app.services.__init__`）时正常运行：

```python
# 新增 stub 项（game_session_store.py / party_store.py / character_store.py 均已删除，仅剩 .pyc）
for _mod_name, _attrs in [
    ("app.services.game_session_store", {"GameSessionStore": object}),
    ("app.services.party_store",        {"PartyStore": object}),
    ("app.services.character_store",    {"CharacterStore": object}),
]: ...
```

> **背景**：这 3 个模块的 `.py` 源文件已在更早的重构中被删除（仅剩 `__pycache__/*.pyc`，Python 不会自动加载无源文件的 `.pyc`）。`admin_coordinator.py` 仍引用它们，是 Phase C 前的技术债。测试中 stub 是临时措施，Phase C 完成后随 `admin_coordinator` 重构一并消除。

---

## Phase C：FlashCPU 溶解

> 高风险：解开万能容器，重建依赖注入链。Phase A+B 完成后执行。

### 背景

FlashCPU 原始使命是「Agentic 会话驱动」，Phase 4 后已被 AgenticExecutor 取代。当前退化为 5 合 1 万能容器：

| 职责 | 行数 | 应归属 |
|------|------|--------|
| `execute_request()` 命令分发 | ~250 | 溶解：gm_extra_tools 直调各 service |
| `call_combat_tool()` MCP 中继 | ~16 | gm_extra_tools / admin_coordinator 直调 MCPClientPool |
| `self.llm_service` 服务持有 | — | pipeline / coordinator 直注 LLMService |
| `self.image_service` 服务持有 | — | pipeline / coordinator 直注 ImageService |
| `_npc_dialogue_*()` 旧 NPC 对话 | ~73 | 废弃（/interact/stream 已替代） |
| `_load_agentic_prompt()` prompt 加载 | ~7 | 迁入 agentic/ |
| `sync_combat_result_to_character()` | ~25 | 内联到 admin_coordinator 调用处 |
| `_build_state_delta / _apply_delta` | ~50 | 归还 StateManager |

### Step C1：LLMService / ImageService 直注

**目标**：pipeline_orchestrator 和 admin_coordinator 不再从 `flash_cpu.llm_service` 取 LLM 服务。

**操作**：

1. `PipelineOrchestrator.__init__` 新增 `llm_service` 和 `image_service` 参数
2. 替换所有 `self.flash_cpu.llm_service` → `self.llm_service`（pipeline 内约 6 处）
3. 替换所有 `self.flash_cpu.image_service` → `self.image_service`（pipeline 内 1 处）
4. `AdminCoordinator` 构造时直接传入 LLMService / ImageService
5. `AdminCoordinator` 中 `self.flash_cpu.llm_service` 引用改为 `self.llm_service`

### Step C2：gm_extra_tools 去 FlashCPU 化 + WorldAPI 扩展

**目标**：`gm_extra_tools.py` 的闭包不再捕获 `flash_cpu`；L1 工具通过 `ctx.api`（WorldAPI）行动，不直调 L2/L3 service。

> **设计原则**：架构哲学要求"上层居民通过 WorldAPI 行动"。gm_extra_tools 属于 L1 层，应通过 `ctx.api` 走 L2 WorldAPI 统一接口，而非直调 `party_service` / `MCPClientPool`。

**操作**：

1. **扩展 WorldAPI**（`app/services/world_api.py`）新增方法覆盖 gm_extra_tools 所需操作：
   - `start_combat(...)` → 委托 MCPClientPool
   - `get_combat_options(...)` / `choose_combat_action(...)` → 委托 MCPClientPool
   - `add_teammate(...)` / `remove_teammate(...)` / `disband_party()` → 委托 party_service
   - `spawn_passerby(...)` → 委托 passerby_service
   - `ability_check(...)` → 委托 AbilityCheckService
2. `build_gm_extra_tools()` 签名变更：去掉 `flash_cpu` 参数，工具内部改为通过 `ctx.api.xxx()` 调用
3. 每个工具内部改造：
   - `npc_dialogue` → **废弃删除**（现代路径 /interact/stream）
   - `start_combat` / `get_combat_options` / `choose_combat_action` → `ctx.api.start_combat()` 等
   - `add_teammate` / `remove_teammate` / `disband_party` → `ctx.api.add_teammate()` 等
   - `ability_check` → `ctx.api.ability_check()`
   - `spawn_passerby` → `ctx.api.spawn_passerby()`
4. `_load_agentic_prompt()` 迁入 `app/agentic/` 作为模块级函数
5. Pipeline 中 `build_gm_extra_tools()` 调用点更新参数
6. **覆盖 pipeline 直调遗漏**：`pipeline_orchestrator.py:1419` 的 `_pipeline_npc_dialogue` 仍走 `flash_cpu.execute_request(NPC_DIALOGUE)`，需改为通过 WorldAPI 或直接废弃（与 `npc_dialogue` 工具同步废弃）

### Step C2b：队友链路去 FlashCPU 化

**目标**：`teammate_response_service`（Phase B 已迁至 `app/agentic/teammate/response_service.py`）的 FlashCPU 依赖解除。

> **Codex 审计发现**：计划原版遗漏了队友链路。`teammate_response_service` 有两处 FlashCPU 依赖需要拆解。

**操作**：

1. 构造注入解除（`:86`）：`__init__` 中的 `flash_cpu` 参数 → 替换为 `mcp_pool`（或通过 WorldAPI）
2. 战斗工具调用（`:1022`）：`flash_cpu.call_combat_tool()` → `ctx.api.xxx()` 或直接 `mcp_pool.call_tool()`
3. 所有构造 `TeammateResponseService` 的调用方更新参数

**涉及文件**：

| 文件 | 操作 |
|------|------|
| `app/agentic/teammate/response_service.py` | 去掉 `flash_cpu` 构造参数 + 替换调用 |
| `app/services/admin/pipeline_orchestrator.py` | 更新 TeammateResponseService 构造 |
| `app/services/admin/admin_coordinator.py` | 更新 TeammateResponseService 构造（如有） |

### Step C3：FlashCPU 残留清理

**操作**：

1. `sync_combat_result_to_character()` 逻辑内联到 `admin_coordinator` 调用处（仅 1 处）
2. `_build_state_delta` / `_apply_delta` 移入 `StateManager`（或内联到 admin_coordinator）
3. `AgenticContext.flash_cpu` 字段删除，受影响文件（Codex 审计补全）：
   - `app/agentic/immersive_tools.py:41`（字段声明）
   - `app/services/admin/pipeline_orchestrator.py`（所有 AgenticContext 构造处，4+ 处传 `flash_cpu=...`）
   - `app/agentic/teammate/response_service.py`（AgenticContext 构造处）
   - `app/agentic/gm_extra_tools.py`（如有引用 `ctx.flash_cpu`）
   - 测试文件中所有 `flash_cpu=` 参数
4. `pipeline_orchestrator` 构造函数去掉 `flash_cpu` 参数
5. `admin_coordinator` 中删除 FlashCPU 实例化
6. 删除 `app/services/admin/flash_cpu_service.py`
7. 更新 `app/services/admin/__init__.py` 和 `app/services/__init__.py`

### Step C4：全量验证

```bash
python3 -m pytest tests/ -q --tb=short --continue-on-collection-errors
```

预期：655 passed（零回归）

---

## 风险与注意事项

### Phase A 风险（低）

- `tiered_ai_service.py` 删除前需确认 `NPCTier` enum 和 `AITier` enum 是否被其他地方引用（`NPCTier` 来自 `worldbook_graphizer/models.py`，应该无影响）
- `memory_graphizer.py` 迁移可能触发 `app/services/__init__.py` 链式导入问题（已知问题 L2-1）

### Phase B 风险（中）

- `teammate_response_service.py` 有 `flash_cpu` 依赖（`_make_combat_action_tool`），Phase B 先保持原样传递，Phase C 再解开
- 1186 行大文件搬迁，import 更新点可能较多

### Phase C 风险（高）

- FlashCPU 是 admin_coordinator 的核心依赖，溶解涉及 admin_coordinator / pipeline_orchestrator / gm_extra_tools 三个大文件
- `npc_dialogue` MCP 工具废弃需确认是否有前端直接调用（通过 REST → FlashCPU 路径）
- `_apply_delta` + `session_store.update_session` 链路需要确认 StateManager 能否完全接管
- 需要逐步执行，每个 Step 后运行测试

### Phase A/B 执行中发现的技术债（Phase C 前需处理）

| 项目 | 文件 | 描述 | 优先级 |
|------|------|------|--------|
| ⚠️ 遗漏 import 更新 | `app/world/npc/instance_manager.py:403` | 仍用 `from app.services.memory_graphizer import MemoryGraphizer`（`.py` 已删，运行时会 ModuleNotFoundError） | **高**（Phase C 前必须修复） |
| 技术债 | `app/services/admin/admin_coordinator.py:38,44,47` | 引用 3 个已删除模块（`game_session_store`、`party_store`、`character_store`），依赖 `.pyc` 残留（不可靠）；测试中用 stub 临时规避 | 中（Phase C 重构时一并处理） |
| 文档未同步 | `全量文件清单与层级归类.md`、`L1-上层Agent地图.md` | `llm_service.py`/`image_generation_service.py` 标记为 L1，实为 Infra；`event_llm_service.py` 路径仍显示旧位置 | 低（文档债） |
| `agentic/__init__.py` | `app/agentic/__init__.py` | 未导出新迁入的 `memory_graphizer`、`event_llm_service`、`passerby_llm`（当前文件仅 docstring，不影响功能） | 低 |

---

## 预期成果

### Phase A 后

- `app/services/` 的 L1 文件：6 → 3（删 1 + 搬 2）
- `tiered_ai_service.py` 218 行死代码清除
- `app/agentic/passerby_llm.py` 新建（L1 路人 LLM 逻辑从 TieredAI 提取，L2 passerby_service 不再混 L1）
- 地图文档更新：llm_service / image_generation 重新定级 Infra

### Phase B 后

- `app/services/` 的 L1 文件：3 → 2（搬 1）
- `app/agentic/` 形成完整子系统：执行器 + 工具 + 记忆图谱化 + 事件NLP + 队友
- 剩余 L1 散件仅 `llm_service`（Infra）和 `image_generation`（Infra）

### Phase C 后

- FlashCPU 删除（503 行），万能容器消失
- `AgenticContext.flash_cpu` 字段删除
- Pipeline / AdminCoordinator 依赖注入链清洁化
- `npc_dialogue` 旧路径废弃
- gm_extra_tools 完全独立，不再依赖 FlashCPU 中间层

### 最终 agentic/ 结构

```
app/agentic/
├── __init__.py
├── agentic_executor.py          # Agent 执行循环
├── immersive_tools.py           # 30 个沉浸式工具 + AgenticContext（flash_cpu 字段已删除）
├── gm_extra_tools.py            # 8→7 个 GM 工具（去掉 npc_dialogue，通过 ctx.api 行动）
├── role_registry.py             # 角色→工具集映射
├── memory_graphizer.py          # 对话→图谱 LLM 转换（Phase A 迁入）
├── event_llm_service.py         # 事件 NLP 3 步管线（Phase A 迁入）
├── passerby_llm.py              # 路人 LLM 响应（Phase A 从 TieredAI 提取）
├── prompt_loader.py             # prompt 加载工具函数（Phase C 从 FlashCPU 提取）
└── teammate/
    ├── __init__.py
    └── response_service.py      # 队友决策 + Agentic 响应（flash_cpu 依赖已解除）
```
