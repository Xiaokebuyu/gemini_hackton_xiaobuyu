# 待办清单（2026-03-01 核查基线，持续更新）

> 基于全子系统对照设计规范核查后整理。测试基线：**792 passed**（2026-03-01 收尾轮完成，含 21 个新测试）。

---

## 一、各子系统完成度总览

| 子系统 | 设计符合度 | 状态 |
|--------|-----------|------|
| 规则层（13 handlers / 63 commands） | **100%** | 生产就绪，无缺口 |
| 适配器层（4+1 端口） | **95%** | 生产就绪，OutputPort 有意不实现 |
| 编排层（11 hooks + pipeline） | **100%** | 核心循环完整，AIOsirisHook LLM 已接通，NarrativePlannerHook LLM 变体已接通（O-3），EncounterDetector 已是 BasicEncounterDetector |
| 叙事层（AgenticExecutor + 19 tools） | **~92%** | 完整 LLM 集成+记忆系统+私聊管线；7 层 context 已接入（N-7 ✅）；traits 过滤已启用（N-5 ✅）|
| 交互层（InteractionService + views） | **~85%** | 6 步管线完整，私聊触发完整，对话选项 DC + LLM 生成已接入（N-6 ✅）|
| 内容层（10 registries） | 功能完整 | 嵌套 dataclass 随消费端补齐 |
| 状态层（10 slices） | **~98%** | Read API 便捷方法已补齐（S-1/S-2/S-4 ✅） |
| 应用层（boundary + 类型） | **~97%** | boundary 收口完成，helper 签名收窄（A-3 局部 ✅） |

---

## 二、待办项（按子系统分组）

### 叙事层（当前 ~75-80%）

| # | 待办 | 严重度 | 依赖 | 备注 |
|---|------|--------|------|------|
| N-1 (Phase 1) | MemoryRetriever Protocol + ContextWindow + L6 改造 | 高 | — | ✅ 已完成（D-N14）：MemoryRetriever 注入边界定义，ContextWindow 数据结构，L6 stub→null（D-N14） |
| N-1 (Phase 2) | MemoryGraphPort 适配器（Null + NetworkX/Firestore） | 高 | N-1 P1 | ✅ 已完成（D-N15）：MemoryGraphPort Protocol、WorldKnowledgeGraph（NetworkX + BFS 扩散激活）、KnowledgeGraphMemoryRetriever、_extract_scene_keywords、完整注入链路 |
| N-1 (Phase 3a) | InstanceManager + LRU 实例池 | 高 | N-1 P2 | ✅ 已完成（D-N16）：InstanceManager LRU 池、conversation_history 注入、write_episode stub 接口、溢出检测链路 |
| N-1 (Phase 3b) | write_episode LLM 图谱化 | 高 | N-1 P3a | ✅ 已完成（D-N17）：RECORD_TRIPLE_TOOL function calling、write_episode LLM 实现、ensure_lore_enriched、5 个动态 EdgeType、deps.py 注入链路 |
| N-1 (Phase 4) | Agent 集成 + GameRuntime 扩展 | 高 | N-1 P3 | ✅ 已完成（D-N18）：L6 hits 注入 NPC/Teammate system prompt，`## Relevant world knowledge` 块，cap=5，向后兼容 |
| N-2 | PrivateChatCoordinator 实现 | 高 | N-1 | ✅ 已完成（D-N19 + D-N20）：Phase A 4 步私聊管线 + Phase B PrivateChatTriggerHook（romance≥60/trust≥50/intimate + FlagSlice 冷静期） |
| N-3 | AgentContextBuilder 集中化 | 中 | 无 | ✅ 关闭（审计确认：所有地方均通过 AgentContextBuilder 单入口，无实质散落，不需改动） |
| N-4 | 七层上下文按角色过滤（L1-L4） | 中 | N-3 | ✅ 已完成（审计+补测）：NPC=None / Teammate=部分(available_milestones only) / GM=full，设计已正确，补充 TestL1RoleFiltering 5 个验证测试 |
| ~~N-5~~ | ~~RoleToolRegistry traits 过滤启用~~ | ~~低~~ | ~~无~~ | ✅ 已完成：AgentTool.applicable_traits property、registry set-based 过滤、executor traits 参数透传、OfferTradeTool=["merchant"]、3 处调用方传 NPC tags；6 新测试 |
| ~~N-6~~ | ~~对话选项 DC 计算集成~~ | ~~低~~ | ~~无~~ | ✅ 已完成：SuggestOptionsTool schema 增 dc 字段、_validate_option() 拷贝 dc、execute() metadata 含 tool 标识、GM prompt 解除 suggest_options 屏蔽、npc_interaction Step 5 LLM 选项优先+静态 fallback；7 新测试 |
| N-7 | D-N13 收尾：AgentContextBuilder 输出接入 executor 主通道 | 中 | 无 | ✅ 已完成（D-N21）：NpcFullContext 单次 retrieve + context_layers 注入 + _serialize_context_layers（L0/L2/L3/L5/L7）；Teammate defer 到 N-7 Phase 2 |
| N-7 Phase 2 | Teammate double-retrieve 修复 + context_layers 注入 | 中 | N-7 | ✅ 已完成（D-N22）：TeammateFull + build_teammate_full_context()；_generate_teammate_reactions() + npc_interaction Step 4 均已接入 |
| N-8 | D-N13 收尾：工具执行层角色级只读隔离 | 低 | N-7 | AgentContext 仍携带完整 world+state；角色可见性是 prompt 层软约束，工具执行层无硬隔离 |
| ~~N-9~~ | ~~D-N13 收尾：L2/L3 Agent 侧与 ContextAssembler 对齐~~ | ~~低~~ | ~~无~~ | ✅ 已完成：_build_l2() 补 dynamic_sub_area_counts、_build_l3() 补 dynamic_sub_areas；4 新测试 |

### 交互层（当前 ~75%）

| # | 待办 | 严重度 | 依赖 | 备注 |
|---|------|--------|------|------|
| ~~I-1~~ | ~~NpcInteractionCoordinator 类实现~~ | ~~高~~ | ~~叙事层 Agent 稳定~~ | ✅ 已完成：NpcInteractionCoordinator 6 步管线（D-I02），包含 Steps 1-6 |
| ~~I-2~~ | ~~交互中 NPC Agent 响应（LLM 对话）~~ | ~~高~~ | ~~I-1, N-3~~ | ✅ 已包含在 I-1 Step 2 中（NPC Agent Response） |
| ~~I-3~~ | ~~交互中 GM 旁观 + Teammate 反应~~ | ~~高~~ | ~~I-1~~ | ✅ 已包含在 I-1 Steps 3-4 中（GM 观察 + Teammate 旁观） |
| ~~I-4~~ | ~~对话选项 LLM 生成 + DC 计算~~ | ~~中~~ | ~~N-6~~ | ✅ 已完成（随 N-6）：GM interaction prompt 允许 suggest_options，Step 5 从 gm_result.tool_results 提取 LLM 选项（含 dc），无则静态 fallback |
| ~~I-5~~ | ~~交互时间消耗接入 accumulate~~ | ~~低~~ | ~~I-1~~ | ✅ 已包含在 I-1 Step 6 中（time_cost=1/6，由调用方接入 accumulate） |

### 编排层（当前 ~100%）

| # | 待办 | 严重度 | 依赖 | 备注 |
|---|------|--------|------|------|
| O-1 | AdminCoordinator 抽象引入 | 低 | I-1 | 等 3+ coordinator 类型时再做（D-O01） |
| ~~O-2~~ | ~~EventEngine 独立模块化~~ | ~~低~~ | ~~无~~ | ✅ 已完成：新建 event_engine.py（Protocol + BasicEventConditionEvaluator + dataclasses + helpers），event_condition.py 精简为纯 Hook；2 新测试 |
| ~~O-3~~ | ~~NarrativePlanner LLM 变体~~ | ~~低~~ | ~~无~~ | ✅ 已完成：NarrativePlanner.plan() async 化、Protocol async、AgenticNarrativePlanner（narrators.py）+ fallback + deps.py 注入；4 新测试 |
| ~~O-4~~ | ~~NpcScheduleProvider 每日时刻表~~ | ~~低~~ | ~~无~~ | ✅ 已完成：CharacterTemplate.schedule 字段、BasicNpcScheduleProvider._scheduled_destination()、_moves_to_town/_moves_to_home 接入 per-character schedule 优先+全局规则 fallback；9 新测试 |
| O-5 | EncounterDetector 实际触发逻辑 | 低 | 无 | ✅ 已完成（文档误记）：EncounterHook.__init__ 已默认 BasicEncounterDetector；encounter_check handler 完整；hook 级集成测试存在 |

### 内容层

| # | 待办 | 严重度 | 依赖 | 备注 |
|---|------|--------|------|------|
| ~~C-1~~ | ~~Registry dataclass 化（分批）~~ | ~~中~~ | ~~无~~ | ✅ 已完成：10/10 registry，18 个 dataclass（Batch 0~6B），嵌套 schema 随子系统深化补齐 |

**各 Registry 覆盖度**：

| Registry | dataclass 覆盖 | 缺失子结构 |
|----------|---------------|-----------|
| ItemRegistry | 50% | WeaponData, ArmorData, AccessoryData, ConsumableData |
| CharacterRegistry | 60% | Attack, Schedule, BaseDisposition |
| MapRegistry | 40% | SubLocationTemplate, EncounterEntry, ContainerData, TrapData |
| SkillRegistry | 75% | StatusEffectTemplate 完整 schema |
| QuestRegistry | 80% | MilestoneCondition, FailureCondition |
| FactionRegistry | 40% | BehavioralRule, FactionRelation |
| LoreRegistry | 30% | WorldRule 整体结构 |
| TagRegistry | 70% | Dimension structured |
| MonsterRegistry | 70% | AI behavior 字段 |
| ClassRegistry | 85% | Feature.type enum |

### 状态层

| # | 待办 | 严重度 | 依赖 | 备注 |
|---|------|--------|------|------|
| ~~S-1~~ | ~~PartySlice Read API~~ | ~~低~~ | ~~无~~ | ✅ 已完成：get_members() / get_approval() / get_shared_experiences()；3 新测试 |
| ~~S-2~~ | ~~RelationSlice Read API~~ | ~~低~~ | ~~无~~ | ✅ 已完成：get_disposition() / get_stage() / get_faction() / get_shop_state()（两层拷贝） / reduce_stock()；11 新测试 |
| ~~S-3~~ | ~~EventSlice 架构对齐~~ | ~~中~~ | ~~无~~ | ✅ 已完成（D-S04）：trigger_condition 完整迁移，check_triggers()/resolve() 实现，旧 trigger_tick 向后兼容 |
| ~~S-4~~ | ~~AreaSlice 便捷方法~~ | ~~低~~ | ~~无~~ | ✅ 已完成（代码早已存在）：count_dynamic_sub_areas()（line 466）/ has_cluster_capacity()（line 479）已实现并被 narrative_planner.py 调用 |

### 应用层

| # | 待办 | 严重度 | 依赖 | 备注 |
|---|------|--------|------|------|
| ~~A-1~~ | ~~main.py God Module 拆分~~ | ~~中~~ | ~~无~~ | ✅ 已完成：拆为 routers/{character,sessions,gameplay,panels}.py，main.py 缩至 10 行 |
| ~~A-2~~ | ~~伪流式 → 真流式~~ | ~~中~~ | ~~无~~ | ✅ 已完成（D-A02）：LLM 最终轮真流式 + thought_signature 修复；interact/private_chat 端点改为 task+queue 模式 |
| ~~A-3~~ | ~~应用层类型约束收窄（局部）~~ | ~~低~~ | ~~无~~ | ✅ 已完成（局部）：interaction_service.py _normalized_text / _normalized_id 签名 Any→object；其余 app/ Any 有意保留（动态载荷合理） |
| ~~A-4~~ | ~~GameRuntime boundary 收口~~ | ~~中~~ | ~~无~~ | ✅ 已完成（2026-03-01）：三个 app 层工厂方法移至 deps.py，GameRuntime 改为纯注入模式，game_core 对 app 层零依赖 |
| ~~A-5~~ | ~~屎山治理 Phase A-D~~ | ~~中~~ | ~~无~~ | ✅ 已完成（2026-03-01）：A=私聊失败语义修正+ThrowingLlmProvider 测试；B=interaction_service 16 静态方法提取；C=runtime+deps 11处Any→具体类型；D=ruff+README |

### 测试与数据

| # | 待办 | 严重度 | 依赖 | 备注 |
|---|------|--------|------|------|
| ~~T-1~~ | ~~端到端集成测试~~ | ~~中~~ | ~~无~~ | ✅ 已完成（D-T01）：6 个 E2E 测试覆盖 10 tick 生命周期（tests/test_game_e2e_session.py） |
| T-2 | 数据管线接入 | 低 | 运行时契约稳定 | data/goblin_slayer/structured_new 存在但未接入（CODEX_INIT 策略：先稳定契约） |

---

## 三、依赖关系图

```
N-1 (MemoryGraph)
 └──→ N-2 (PrivateChatCoordinator)

N-3 (AgentContextBuilder)
 ├──→ N-4 (角色过滤)
 └──→ I-2 (交互 Agent 响应)

I-1 (NpcInteractionCoordinator)
 ├──→ I-2 (Agent 响应)
 ├──→ I-3 (GM/Teammate 反应)
 ├──→ I-4 (对话选项)
 └──→ I-5 (时间消耗)

I-1 ──→ O-1 (AdminCoordinator，等 3+ coordinator)
```

---

## 四、建议推进顺序（2026-03-01 更新）

> N-5 + O-3 已完成，以下为当前剩余待办的推荐顺序。

**当前优先级（功能深化）**：
1. **N-6 / I-4** — 对话选项 DC 计算接入（完成叙事规范 §五 闭环）
2. **O-4** — NpcScheduleProvider 更丰富时刻表

**低优先级 / 有意推迟**：
3. **N-8/N-9** — D-N13 工具层隔离 + L2/L3 对齐
4. **S-1/S-2/S-4** — 状态层 Read API 便捷方法
5. **A-3** — 剩余 Any 类型收窄
6. **T-2** — goblin_slayer 数据管线（等运行时合约稳定）
7. **O-2** — EventEngine 独立模块化（命名清理）
