# 待决策问题

实施过程中发现的需要用户决策的问题，批量处理。

---

## Q1: Clue check 由谁执行？ → 已决策：方案 A（GM 对话流 + apply_clue_check_result）

---

## Q2: 好感折扣是否应该双向？

**背景**：当前 `_approval_discount` 只应用在买入价（`_resolve_buy_unit_price`），卖出价（`_resolve_sell_unit_price`）不受好感影响。好感高只能买便宜，卖价不变。

**问题**：是否应该让高好感时卖价也更好？（比如 approval≥80 时 buy_rate 从 0.5 提高到 0.65）

**我的倾向**：加上，让好感双向受益。简单改动。
