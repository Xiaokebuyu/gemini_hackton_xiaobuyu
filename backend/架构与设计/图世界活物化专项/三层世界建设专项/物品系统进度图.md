# 物品系统进度图

更新时间：2026-02-25
关联文档：[物品类基础系统设计.md](物品类基础系统设计.md) · [物品数据 Schema 规格.md](物品数据Schema规格.md)

---

## 总览

```
数据层 ░░░░░░░░░░░░░░░░░░░░  待管线重提取
基础层 ██░░░░░░░░░░░░░░░░░░  Step 1 代码就绪，等数据
桥接层 ░░░░░░░░░░░░░░░░░░░░  未开始
丰富层 ░░░░░░░░░░░░░░░░░░░░  未开始
```

---

## Step 详情

| Step | 名称 | 状态 | 产出 | 阻塞 |
|------|------|------|------|------|
| 0 | 物品数据迁移 | 🔴 **阻塞中** | items.json → BG3 Schema | 等管线提取 prompt 更新 |
| 1 | 子系统 A — 物品注册表 | ⏸️ 代码就绪 | `app/models/item.py` ✅ · `app/world/item/registry.py` ✅ · parse_price ✅ · subtype 过滤 ✅ | 等 Step 0 数据 |
| 2 | 金币补全 | ⬜ 待做 | StatOps.remove_gold + Immersive tools | Step 1 |
| 3 | 子系统 B — 背包打通 | ⬜ 待做 | REST API + drop_item | Step 2 |
| 4 | 子系统 C — 装备打通 | ⬜ 待做 | AC 重算适配 base_ac + StatOps equip/unequip + REST + Immersive | Step 3 |
| 5 | 子系统 D — 物品效果 | ⬜ 待做 | special_effects 执行 + use_item 全链路 | Step 4 |
| 6 | 桥接 E — 状态系统 | ⬜ 待做 | conditions CRUD | Step 5 |
| 7 | 桥接 F — 战斗掉落 | ⬜ 待做 | roll_loot（可与 2-6 并行） | Step 1 |
| 8 | 桥接 G — 装备熟练度 | ⬜ 待做 | check_proficiency | Step 4 |

```
状态图例：✅ 完成  ⏸️ 代码就绪/挂起  ⬜ 待做  🔴 阻塞
```

---

## 已完成的代码产出

| 文件 | 类型 | 说明 |
|------|------|------|
| `app/models/item.py` | 新建 | Item Pydantic 模型 + WeaponData/ArmorData/ConsumableData/SpecialEffect + 全部枚举（ItemType, Rarity, DamageType, EquipSlot, EffectTrigger, EffectType） |
| `app/world/item/__init__.py` | 新建 | 子系统包入口，导出 get_item/list_items/search_items/parse_price |
| `app/world/item/registry.py` | 新建 | ItemRegistry — 加载 items.json、解析为 Item 模型、查询接口、parse_price 价格解析 |
| `app/world/player/__init__.py` | 更新 | import 指向 `app.world.item.registry` |
| `app/models/player_character.py` | 更新 | import 指向 `app.world.item.registry` |
| `app/world/player/node_view.py` | 更新 | import 指向 `app.world.item.registry` |
| `app/world/player/operations.py` | 更新 | import 指向 `app.world.item.registry` |
| `app/world/player/item_registry.py` | 删除 | 旧 registry，已迁移至 `app/world/item/` |

---

## 依赖图

```
Step 0（数据迁移）🔴
    └──→ Step 1（注册表）⏸️
            ├──→ Step 2（金币）──→ Step 3（背包）──→ Step 4（装备）──→ Step 5（效果）──→ Step 6（状态）
            ├──→ Step 7（战斗掉落，可并行）
            └──→ Step 4 ──→ Step 8（熟练度，可并行）
```

关键路径：**0 → 1 → 2 → 3 → 4 → 5 → 6**

---

## 解除阻塞条件

Step 0 解除需要以下任一：

1. **管线重提取**：更新 `worldbook_graphizer` 的提取 prompt，让 LLM 按新 Schema 输出 items.json
2. **手动编写测试数据**：按新 Schema 手写一批物品 JSON（仅用于开发调试，正式数据仍走管线）

> Step 2（金币补全）不依赖物品数据，理论上可以先做。但按原计划整体挂起，等数据就绪后一起推进。

---

## 变更日志

| 日期 | 变更 |
|------|------|
| 2026-02-25 | 创建。记录 Step 0-8 进度 + 已完成代码产出 + 阻塞状态 |
