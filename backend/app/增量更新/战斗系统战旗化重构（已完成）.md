wo'men# 战斗系统战旗化重构（SRPG）

创建时间：2026-03-10
状态：调查完成，待设计定调
前置：游戏流程调查审计 §4.1~4.5

---

## 一、现状分析

### 1.1 当前战斗系统画像

`combat.py`（1296 行）实现了一套**无空间、玩家先手、原子回合制**战斗：

| 维度 | 现状 | 目标 |
|------|------|------|
| 空间模型 | **无** — 零坐标、零距离检查 | 正方格网格 + 地形 |
| 回合结构 | 玩家行动 → 所有怪物原子反击 | 速度制交替行动（先攻队列） |
| 行动经济 | 1 action = 全回合 | 移动 + 行动 分离 |
| 参战单位 | 仅玩家 vs 怪物 | 玩家 + 队友 vs 怪物（3v5） |
| 怪物 AI | 2 种性格决策树（attack/flee） | 规则 AI（A* + 地形）+ 精英 LLM |
| 地形效果 | 无 | 8 种地形 + 全局环境 |
| 远程/近战 | `attacks[].range` 存在但**不做距离检查** | 距离决定可否攻击 |

### 1.2 可复用的模块

以下核心逻辑可在重构中保留：

| 模块 | 位置 | 复用方式 |
|------|------|---------|
| d20 攻击判定 | `_roll_monster_attack`, `_compute_attack_resolution` | 命中公式沿用（d20 + mod + prof vs AC） |
| 伤害减免链 | `_apply_player_damage_resistance` | resistance/immunity/vulnerability 直接复用 |
| 状态效果系统 | `status_effect.py` + `active_effects` | 效果结构 + tick 逻辑完整 |
| 怪物模板数据 | `monsters.json` | attacks 已有 range/damage_dice/tags |
| XP 结算 | `_compute_combat_xp` | 击杀 XP 计算直接复用 |
| dice 工具 | `skill_check.py` 的 `resolve_roll` | d20 + 优劣势完整 |

### 1.3 必须重写的部分

| 模块 | 原因 |
|------|------|
| 回合控制流 | 从"玩家→全怪物"改为先攻队列逐单位行动 |
| `start_combat` | 需初始化网格、放置单位、生成先攻序 |
| `_resolve_monster_responses` | 拆解为单怪物回合（含移动+行动） |
| `advance_combat_round` | 改为推进先攻队列中的下一个单位 |
| 所有 player_flags 逻辑 | defending/disengaged/dashed 改为 per-unit 状态 |

### 1.4 StateChange 路径现状

当前战斗状态全部存在 AreaSlice：

```python
# 战斗状态（整体替换 payload）
StateChange("areas", "modify", f"hostile_tracking.{sub_area_id}", updated_payload)

# 玩家 HP
StateChange("player", "set", "hp", new_hp)

# 玩家 XP
StateChange("player", "set", "xp", new_xp)

# 击杀计数
StateChange("flags", "set", f"flags.kill_count_{monster_id}", count + 1)
```

**设计决策**：战斗状态仍存 `hostile_tracking`，但 payload 结构需扩展（加 grid/turn_order/unit positions）。不新增 slice。

---

## 二、目标架构

### 2.1 核心设计参数（照搬审计文档 §4.1）

| 参数 | 决策 |
|------|------|
| 网格类型 | 正方格（8×6 典型尺寸） |
| 回合结构 | 速度制交替行动（先攻队列） |
| 规则体系 | 沿用 D&D 5e（命中/伤害/AC） |
| 单位规模 | 最大 3v5（含玩家） |
| 战斗节奏 | 高攻低防，3-5 轮结束 |

### 2.2 战斗 payload 扩展结构

```python
hostile_payload = {
    # --- 原有字段（保留） ---
    "area_id": str,
    "sub_area_id": str,
    "combat_active": bool,
    "cleared": bool,
    "blocking": bool,
    "combat_round": int,
    "surprise_state": str,
    "monster_ids": list[str],
    "combat_started_at_tick": int | None,
    "cleared_at_tick": int | None,
    "status": str,
    "source": str,

    # --- 新增：网格 ---
    "grid": {
        "width": int,           # 列数（如 8）
        "height": int,          # 行数（如 6）
        "terrain": list[str],   # 行×列，每格一个字符（G/F/M/H/S/W/R/B）
        "legend": dict,         # 字符→地形名映射
    },

    # --- 新增：全局环境 ---
    "environment": {
        "time_of_day": str,     # "day" | "night"
        "weather": str,         # "clear" | "rain" | "fog"
    },

    # --- 重构：单位列表（统一玩家+队友+怪物） ---
    "units": [
        {
            "unit_id": str,             # "player" / "priestess" / "goblin_1"
            "side": str,                # "ally" | "enemy"
            "source": str,              # "player" | "companion" | "monster"
            "monster_id": str | None,   # 怪物模板 ID（仅 monster）
            "character_id": str | None, # 角色 ID（仅 companion）
            "name": str,
            "hp": int,
            "max_hp": int,
            "ac": int,
            "stats": dict[str, int],    # str/dex/con/int/wis/cha
            "speed": int,               # 移动力（格数）
            "position": [int, int],     # [col, row]
            "alive": bool,
            "fled": bool,
            "active_effects": list[dict],
            "attacks": list[dict],      # 同 MonsterTemplate.attacks 格式
            "ai_personality": str | None,  # 怪物 AI 性格
            "flee_threshold": float,
            "flee_chance": float,
            "action_used": bool,        # 本回合是否已行动
            "move_used": bool,          # 本回合是否已移动
            "proficiency_bonus": int,
        }
    ],

    # --- 新增：先攻队列 ---
    "turn_order": list[str],    # unit_id 序列，按先攻值降序
    "current_turn_index": int,  # 当前行动单位在 turn_order 中的位置
    "current_unit_id": str,     # 冗余字段，方便查询

    # --- 废弃 ---
    # "participants": [...],     # 被 units 替代
    # "player_flags": {...},     # 被 per-unit action_used/move_used 替代
}
```

### 2.3 地形系统

| 代码 | 地形 | 移动消耗 | 战斗效果 | 可通行 |
|------|------|---------|---------|--------|
| G | 草地 | 1 | 无 | ✓ |
| F | 森林 | 2 | +20% 回避（AC +2） | ✓ |
| H | 丘陵 | 2 | 远程攻击 +1 射程 | ✓ |
| S | 沼泽 | 3 | 速度 -2 | ✓ |
| W | 水面 | ∞ | — | ✗（浅水=2） |
| R | 石地/废墟 | 1 | 防御 +1（AC +1） | ✓ |
| B | 断墙 | ∞ | 阻挡远程攻击 LoS | ✗ |
| M | 山地 | ∞ | — | ✗ |

```python
# 地形常量（新文件 battle_grid.py）
@dataclass(slots=True, frozen=True)
class TerrainType:
    code: str
    name: str
    move_cost: int          # 0 = 不可通行
    ac_bonus: int
    range_bonus: int
    blocks_los: bool        # 阻挡远程视线
    speed_penalty: int      # 站在此地形时速度惩罚

TERRAIN_REGISTRY: dict[str, TerrainType] = {
    "G": TerrainType("G", "grass",    1, 0, 0, False, 0),
    "F": TerrainType("F", "forest",   2, 2, 0, False, 0),
    "H": TerrainType("H", "hill",     2, 0, 1, False, 0),
    "S": TerrainType("S", "swamp",    3, 0, 0, False, 2),
    "W": TerrainType("W", "water",    0, 0, 0, False, 0),
    "R": TerrainType("R", "stone",    1, 1, 0, False, 0),
    "B": TerrainType("B", "wall",     0, 0, 0, True,  0),
    "M": TerrainType("M", "mountain", 0, 0, 0, True,  0),
}
```

### 2.4 全局环境效果

```python
ENVIRONMENT_EFFECTS = {
    "night":  {"range_penalty": -2, "ranged_hit_penalty": -2},   # 视野-2格，远程命中-10%≈-2
    "rain":   {"fire_damage_mult": 0.5, "lightning_damage_mult": 1.5},
    "fog":    {"range_penalty": -1},
}
```

### 2.5 先攻（Initiative）系统

```python
def roll_initiative(unit: dict) -> int:
    """d20 + DEX modifier。速度制交替行动。"""
    dex_mod = (unit["stats"]["dex"] - 10) // 2
    roll = random.randint(1, 20)
    return roll + dex_mod

def build_turn_order(units: list[dict]) -> list[str]:
    """按先攻值降序排列所有存活单位。同值时 DEX 高者优先。"""
    initiatives = []
    for u in units:
        if u["alive"] and not u["fled"]:
            init_val = roll_initiative(u)
            initiatives.append((init_val, u["stats"]["dex"], u["unit_id"]))
    initiatives.sort(key=lambda x: (x[0], x[1]), reverse=True)
    return [uid for _, _, uid in initiatives]
```

### 2.6 单位构建

```python
def build_player_unit(state: StateContainer) -> dict:
    """从 PlayerSlice 构建玩家战斗单位。"""
    p = state.player
    weapon = p.equipment.get("weapon")
    attacks = []
    if weapon:
        attacks.append({
            "name": weapon["name"],
            "hit_bonus": p.get_modifier("str") + p.proficiency_bonus,
            "damage_dice": weapon.get("damage_dice", "1d6"),
            "damage_type": weapon.get("damage_type", "physical"),
            "range": weapon.get("range", 1),
        })
    else:
        attacks.append({
            "name": "徒手攻击",
            "hit_bonus": p.get_modifier("str") + p.proficiency_bonus,
            "damage_dice": "1d1",
            "damage_type": "bludgeoning",
            "range": 1,
        })
    return {
        "unit_id": "player",
        "side": "ally",
        "source": "player",
        "monster_id": None,
        "character_id": None,
        "name": p.name,
        "hp": p.hp,
        "max_hp": p.max_hp,
        "ac": p.ac,
        "stats": dict(p.stats),
        "speed": 3,  # 默认移动力 3 格
        "position": None,  # start_combat 时分配
        "alive": True,
        "fled": False,
        "active_effects": list(p.active_effects),
        "attacks": attacks,
        "ai_personality": None,
        "flee_threshold": 0.0,
        "flee_chance": 0.0,
        "action_used": False,
        "move_used": False,
        "proficiency_bonus": p.proficiency_bonus,
    }

def build_companion_unit(character_id: str, member_data: dict, template: CharacterTemplate) -> dict:
    """从 PartySlice member + CharacterTemplate 构建队友战斗单位。"""
    # 队友的 stats/hp/attacks 来源：
    # - 优先从 member_data（运行时状态）
    # - fallback 到 template（角色模板）
    ...

def build_monster_unit(monster_id: str, template: MonsterTemplate, index: int) -> dict:
    """从 MonsterTemplate 构建怪物战斗单位。"""
    return {
        "unit_id": f"{monster_id}_{index}",
        "side": "enemy",
        "source": "monster",
        "monster_id": monster_id,
        ...
        "speed": template.speed if hasattr(template, "speed") else 2,
        "attacks": [asdict(a) for a in template.attacks],
        "ai_personality": template.ai_personality,
        ...
    }
```

### 2.7 命令类型重设计

```python
# 废弃/合并
# - "advance_combat_round" → 自动推进，无需命令
# - "defend" / "disengage" / "dash" → 统一为行动选项

# 新命令
COMMAND_TYPES = (
    "start_combat",           # 初始化战斗（engine-only）
    "combat_move",            # 移动到目标格（消耗移动力）
    "combat_attack",          # 攻击目标单位（消耗行动）
    "combat_defend",          # 防御姿态（消耗行动，AC+2 到下回合）
    "combat_skill",           # 使用技能/法术（消耗行动）
    "combat_use_item",        # 使用消耗品（消耗行动）
    "combat_shove",           # 推撞（消耗行动）
    "combat_flee",            # 逃跑（消耗行动+移动）
    "combat_stand_up",        # 起身（消耗移动力的一半）
    "combat_end_turn",        # 主动结束回合
    # engine-only
    "combat_npc_turn",        # NPC/怪物回合（engine 触发）
)
```

### 2.8 单回合流程

```
当前单位的回合开始
  │
  ├─ 若为玩家 → 前端展示：可移动范围 + 可用行动
  │   ├─ [可选] combat_move → 移动到目标格
  │   ├─ [必选其一] combat_attack / combat_defend / combat_skill / combat_use_item / combat_shove / combat_flee
  │   └─ combat_end_turn（或行动+移动都用完后自动结束）
  │
  ├─ 若为队友 → engine 触发 combat_npc_turn
  │   └─ LLM 决策（输入：战场状态 + 性格 + 关系）→ 移动 + 行动
  │
  ├─ 若为普通怪 → engine 触发 combat_npc_turn
  │   └─ 规则 AI（A* 寻路 + 地形加权 + 目标选择）→ 移动 + 行动
  │
  └─ 若为精英/BOSS → engine 触发 combat_npc_turn
      └─ LLM 决策（输入：战场状态 + 战术目标）→ 移动 + 行动

回合结束 → 推进 current_turn_index → 若越界则 combat_round++ 并重置 index=0
```

---

## 三、核心新模块设计

### 3.1 BattleGrid（网格 + 寻路）

**文件**：`app/game_core/rules/battle_grid.py`

```python
@dataclass(slots=True)
class BattleGrid:
    width: int
    height: int
    terrain: list[list[str]]  # [row][col] → terrain code

    def at(self, col: int, row: int) -> TerrainType: ...
    def is_passable(self, col: int, row: int) -> bool: ...
    def move_cost(self, col: int, row: int) -> int: ...
    def is_in_bounds(self, col: int, row: int) -> bool: ...
    def occupied_by(self, col: int, row: int, units: list[dict]) -> str | None: ...

    def reachable_cells(
        self, start: tuple[int, int], move_points: int, units: list[dict]
    ) -> set[tuple[int, int]]:
        """BFS：从 start 出发，在 move_points 内可到达的所有格子。"""
        ...

    def shortest_path(
        self, start: tuple[int, int], goal: tuple[int, int], units: list[dict]
    ) -> list[tuple[int, int]] | None:
        """A* 寻路。cost = terrain.move_cost。不可穿过敌方单位。"""
        ...

    def line_of_sight(
        self, start: tuple[int, int], end: tuple[int, int]
    ) -> bool:
        """Bresenham 直线检测：途经 blocks_los 地形则被阻挡。"""
        ...

    def cells_in_range(
        self, center: tuple[int, int], range_: int
    ) -> list[tuple[int, int]]:
        """曼哈顿距离 ≤ range 的所有格子。"""
        ...

    def distance(self, a: tuple[int, int], b: tuple[int, int]) -> int:
        """曼哈顿距离 = |dx| + |dy|。"""
        ...

    @classmethod
    def from_map_data(cls, map_data: dict) -> "BattleGrid":
        """从战斗地图 JSON 构建。"""
        terrain = [[ch for ch in row] for row in map_data["terrain"]]
        return cls(width=map_data["size"][0], height=map_data["size"][1], terrain=terrain)
```

### 3.2 规则 AI（怪物）

**文件**：`app/game_core/rules/battle_ai.py`

```python
def decide_monster_turn(
    unit: dict,
    grid: BattleGrid,
    all_units: list[dict],
    environment: dict,
) -> dict:
    """
    规则 AI 决策。返回 {"move_to": [col, row] | None, "action": str, "target": str | None, ...}

    决策优先级：
    1. 检查是否应逃跑（hp_ratio < flee_threshold）
    2. 评估所有敌方目标（ally 单位）的威胁值
    3. 选择最优攻击（damage / range 匹配当前位置）
    4. 计算移动目标：
       - 近战：A* 向最优目标移动
       - 远程：保持射程内、偏好高地
       - 逃跑：远离所有敌人
    5. 地形偏好：弓箭手→丘陵，近战→森林/石地，全部→避开沼泽
    """
    ...

def evaluate_target_priority(
    attacker: dict,
    targets: list[dict],
    grid: BattleGrid,
) -> list[tuple[str, float]]:
    """
    目标优先级评估。返回 [(unit_id, score)] 降序。

    评分因素：
    - 距离（越近越优先）
    - HP（越低越优先击杀）
    - 威胁（治疗者优先）
    - 可达性（本回合能攻击到的优先）
    """
    ...
```

### 3.3 队友 LLM AI

**文件**：`app/agent_orchestration.py`（扩展现有）

```python
async def decide_companion_combat_turn(
    character_id: str,
    battlefield_summary: str,     # 结构化战场描述
    character_profile: str,       # 性格 + 战斗风格
    relationship_context: str,    # 与玩家的关系
    available_actions: list[dict],  # 可用行动列表
) -> dict:
    """
    LLM 决策队友行动。

    输出格式：
    {"move_to": [col, row], "action": "attack", "target": "goblin_1"}

    LLM 输入：
    - 当前战场网格（ASCII 渲染）
    - 各单位位置/HP/状态
    - 自身可移动范围
    - 自身可用攻击及射程
    - 角色性格（女神官→治疗优先；妖精弓手→保持距离；哥布林杀手→效率至上）
    - 与玩家关系（好感高→保护/治疗玩家优先）

    Fallback：LLM 失败时降级为规则 AI。
    """
    ...
```

### 3.4 战斗地图加载

**文件**：`app/game_core/content/battle_maps.py`

```python
@dataclass(slots=True)
class BattleMapTemplate:
    name: str
    size: tuple[int, int]
    terrain: list[str]
    player_spawn: list[list[int]]
    enemy_spawn: list[list[int]]
    legend: dict[str, str]
    tags: list[str]  # ["forest", "narrow", "open", ...]

class BattleMapRegistry:
    """加载 data/goblin_slayer/v2/battle_maps/*.json"""
    maps: dict[str, list[BattleMapTemplate]]  # category → variants

    def select_map(self, tags: list[str]) -> BattleMapTemplate:
        """按 tags 匹配最佳地图，同类多变体随机选一个。"""
        ...
```

### 3.5 事件 → 战斗联动 + 战前检定

#### 3.5.1 现状断点

当前从事件到战斗的完整链路有 **3 个断点**：

```
EventConditionHook (P50)
  └─ _ALLOWED_COMMAND_TYPES = {set_flag, modify_disposition, modify_approval,
     advance_quest, schedule_event, create_rumor, modify_location,
     add_knowledge, modify_completion, adjust_danger}
     ❌ 无 trigger_encounter / start_combat — 事件无法触发战斗

EncounterHook (P40) ← 独立触发（danger × period_multiplier >= 0.75）
  └─ encounter_spotted SSE → 玩家选 enter
     └─ HostileAreaHandler.enter_hostile → 隐匿骰 → surprise_state
        └─ surprise_state = "player_surprise" | "enemy_surprise" | "none"
           ⚠️ 仅二元（全体玩家方 vs 全体敌方），非 per-unit
           ⚠️ surprise round（第0轮）不限制被突袭方行动

start_combat → _build_participants → participants 列表
  ❌ 无 initiative roll
  ❌ 无 turn_order
  ❌ 无 DEX 排序
```

#### 3.5.2 事件触发战斗

**目标**：event_engine 新增 `trigger_encounter` 命令类型，允许事件直接创建遭遇。

**改动**：`event_engine.py` 的 `_ALLOWED_COMMAND_TYPES` 增加 `trigger_encounter`。

```python
# event_engine.py — EventConditionHook._execute_transition_commands()
# 新增允许命令
"trigger_encounter": {
    # 事件指定怪物组合 + 遭遇参数
    "monster_ids": list[str],       # 必填
    "sub_area_id": str | None,      # 可选，默认当前位置
    "surprise_modifier": int,       # 可选，修正隐匿 DC（如伏击 -5）
    "map_tags": list[str],          # 可选，指定战斗地图类型
    "blocking": bool,               # 默认 True
    "narrative": str,               # 遭遇描述文本
}
```

**执行流程**：
```
事件触发 → trigger_encounter 命令
  → 创建 hostile_tracking entry（status="spotted"，同 EncounterHook）
  → 发送 encounter_spotted SSE（含 narrative）
  → 后续流程复用现有 enter_hostile → stealth → start_combat
```

**用例**：
- 里程碑事件触发 BOSS 战（"进入废墟深处时遭遇哥布林萨满"）
- 任务事件触发特定遭遇（"调查失踪案线索 → 触发伏击"）
- 世界事件触发区域性战斗（"哥布林入侵边境小镇"）

#### 3.5.3 突袭检定深化

**现状**：`HostileAreaHandler.enter_hostile` 做一次隐匿骰（d20 + stealth vs DC），输出二元 surprise_state。

**目标**：升级为 **per-unit 突袭判定**，支持"部分怪物被突袭、部分不被突袭"的细粒度。

```python
# 突袭检定流程（在 start_combat 中执行，而非 enter_hostile）
def resolve_surprise(
    units: list[dict],
    base_surprise_state: str,  # 来自 enter_hostile 的隐匿骰结果
) -> list[dict]:
    """
    基于 enter_hostile 的整体突袭状态，为每个单位标记 surprised。

    规则：
    - "player_surprise" → 所有敌方 surprised=False，所有友方 surprised=True
      ...不对，应该是 player 方突袭了敌方：
    - "player_surprise" → 敌方全体 surprised=True，友方不受影响
    - "enemy_surprise" → 友方全体 surprised=True，敌方不受影响
    - "none" → 无人被突袭

    扩展（可选）：per-unit perception 骰对抗
    - 即使整体被突袭，perception 高的怪物可以"警觉"
    - 骰 d20 + perception_bonus vs 隐匿方的 stealth 总值
    - 通过则该怪物 surprised=False
    """
    for unit in units:
        if base_surprise_state == "player_surprise" and unit["side"] == "enemy":
            # 被突袭方可以尝试 perception 对抗
            perception_bonus = (unit["stats"].get("wis", 10) - 10) // 2
            # 简化：高感知怪物有概率不被突袭
            unit["surprised"] = True  # 默认被突袭
        elif base_surprise_state == "enemy_surprise" and unit["side"] == "ally":
            unit["surprised"] = True
        else:
            unit["surprised"] = False
    return units
```

**Surprise Round（第0轮）强制执行**：
```python
# combat_npc_turn / combat_attack 中检查
if payload["combat_round"] == 0 and unit["surprised"]:
    # 被突袭的单位在第 0 轮不能行动
    # 自动 skip → end_turn
    return skip_turn_result(unit)

# 第 0 轮结束后，清除所有 surprised 标记
if new_round == 1:
    for u in payload["units"]:
        u["surprised"] = False
```

**unit 新增字段**：
```python
{
    "surprised": bool,  # 第 0 轮是否被突袭（第 1 轮起自动清除）
}
```

#### 3.5.4 先攻检定

**现状**：完全缺失。

**目标**：`start_combat` 时为每个单位掷先攻（d20 + DEX mod），生成 `turn_order`。

```python
def roll_initiative(unit: dict) -> tuple[int, int]:
    """
    Returns (initiative_total, dex_score) for tiebreaking.
    initiative = d20 + (dex - 10) // 2
    """
    dex = unit["stats"].get("dex", 10)
    dex_mod = (dex - 10) // 2
    roll = random.randint(1, 20)
    return (roll + dex_mod, dex)

def build_turn_order(units: list[dict]) -> tuple[list[str], dict[str, int]]:
    """
    为所有存活单位掷先攻，返回 (排序后的 unit_id 列表, {unit_id: initiative_total})。
    排序：initiative 降序，同值 DEX 高者优先，再同值随机。
    """
    rolls = {}
    for u in units:
        if u["alive"] and not u["fled"]:
            total, dex = roll_initiative(u)
            rolls[u["unit_id"]] = total

    sorted_ids = sorted(
        rolls.keys(),
        key=lambda uid: (rolls[uid], next(u["stats"]["dex"] for u in units if u["unit_id"] == uid)),
        reverse=True
    )
    return sorted_ids, rolls
```

**先攻结果存储**：
```python
hostile_payload["turn_order"] = ["goblin_killer", "player", "goblin_1", "priestess", "goblin_2"]
hostile_payload["initiative_rolls"] = {
    "goblin_killer": 22,  # d20(17) + DEX mod(5)
    "player": 15,
    "goblin_1": 14,
    "priestess": 12,
    "goblin_2": 8,
}
hostile_payload["current_turn_index"] = 0
hostile_payload["current_unit_id"] = "goblin_killer"
```

**SSE 事件**：
```python
SSEEvent("initiative_rolled", {
    "turn_order": [...],
    "rolls": {unit_id: total, ...},
    "current_unit_id": str,
})
```

#### 3.5.5 完整战前序列（重构后）

```
触发源（三种入口）：
  A. EncounterHook（随机遭遇，danger × period）
  B. 事件系统（trigger_encounter 命令）
  C. 脚本触发（enter_hostile 直接调用）
      │
      ▼
encounter_spotted SSE → 玩家选择 enter / retreat
      │ enter
      ▼
enter_hostile → 隐匿骰（d20 + stealth vs DC）
  → base_surprise_state = "player_surprise" | "enemy_surprise" | "none"
  → stealth_result SSE（隐匿结果 + 叙述）
      │
      ▼
start_combat（engine-only）
  ├─ 1. 加载战斗地图（BattleMapRegistry.select_map(tags)）
  ├─ 2. 构建 BattleGrid
  ├─ 3. 构建 units（player + companions + monsters）
  ├─ 4. 分配初始位置（spawn points）
  ├─ 5. resolve_surprise(units, base_surprise_state)  ← 突袭检定
  │     → 标记每个 unit.surprised
  ├─ 6. build_turn_order(units)  ← 先攻检定
  │     → turn_order + initiative_rolls
  ├─ 7. combat_round = 0 if any_surprised else 1
  └─ 8. 发送 SSE：combat_started + initiative_rolled
      │
      ▼
战斗循环开始（第 0 轮 = surprise round，被突袭者跳过行动）
```

---

## 四、分阶段实施计划

### Phase 1：网格基础 + 数据结构（~250 行新增）

**目标**：BattleGrid 类 + 地形常量 + 寻路，纯逻辑无 handler 改动。

**新建文件**：
- `app/game_core/rules/battle_grid.py` — BattleGrid dataclass + TerrainType + TERRAIN_REGISTRY + BFS/A*/LoS

**测试**：
- 地形注册表完整性
- BFS 可达性（含不可通行格/地形消耗/友军不阻挡/敌军阻挡）
- A* 寻路（最短路径 + 绕障 + 无路径返回 None）
- LoS 检测（穿墙阻挡 / 无障碍通过）
- 曼哈顿距离
- `from_map_data` 构建

**不做**：不改 combat.py，不改 hostile_payload 结构。

---

### Phase 2：战斗 payload 重构 + start_combat 改造 + 战前检定（~300 行修改）

**目标**：start_combat 生成新格式 payload（units + grid + turn_order），含完整突袭检定 + 先攻检定。旧 participants 废弃。

**修改文件**：
- `combat.py` — `_compute_start_combat` 重写：
  - 构建 BattleGrid（从地图模板或生成默认地图）
  - 构建 units 列表（build_player_unit + build_companion_unit + build_monster_unit）
  - 分配初始位置（player_spawn / enemy_spawn）
  - `resolve_surprise(units, base_surprise_state)` — per-unit 突袭标记
  - `build_turn_order(units)` — d20 + DEX mod 先攻检定
  - combat_round = 0 if any surprised else 1
  - 输出新格式 payload
- `area.py` — 扩展 `build_combat_hostile` 支持新 payload 格式
- `defaults.py` — 更新命令注册

**新建文件**：
- `app/game_core/rules/combat_units.py` — build_player_unit / build_companion_unit / build_monster_unit / resolve_surprise / build_turn_order / roll_initiative

**兼容策略**：
- 新 payload 有 `"version": 2` 标记
- 旧战斗（无 version）走旧逻辑（短期兼容，后续删除）

**测试**：
- start_combat 生成包含 grid/units/turn_order/initiative_rolls 的 payload
- 玩家单位正确从 PlayerSlice 构建
- 队友单位正确从 PartySlice + CharacterTemplate 构建
- 怪物单位正确从 MonsterTemplate 构建
- 先攻排序正确（d20 + DEX mod，同值 DEX 高者优先）
- 初始位置在 spawn 点内
- player_surprise → 敌方全体 surprised=True
- enemy_surprise → 友方全体 surprised=True
- none → 无人 surprised，combat_round=1

---

### Phase 3：移动命令 + 回合推进 + 突袭轮执行（~250 行修改）

**目标**：实现 `combat_move` + `combat_end_turn` + 自动回合推进 + surprise round 强制跳过。

**修改文件**：
- `combat.py` — 新增 command handler：
  - `combat_move`：验证目标格可达（BFS check）→ 更新 unit.position → 标记 move_used
  - `combat_end_turn`：推进 current_turn_index → 若回合结束则 combat_round++
  - 回合开始时重置 action_used/move_used
  - **surprise round 执行**：combat_round==0 时，surprised 单位自动 skip → end_turn
  - **surprise 清除**：进入 combat_round==1 时，清除所有 unit.surprised

**核心逻辑**：

```python
def _compute_combat_move(self, state, world, params):
    unit = find_current_unit(payload)
    assert unit["unit_id"] == payload["current_unit_id"]
    assert not unit["move_used"]
    target = (params["col"], params["row"])
    grid = BattleGrid.from_payload(payload["grid"])
    reachable = grid.reachable_cells(unit["position"], unit["speed"], payload["units"])
    assert target in reachable
    unit["position"] = list(target)
    unit["move_used"] = True
    # 返回 StateChange

def _compute_combat_end_turn(self, state, world, params):
    idx = payload["current_turn_index"] + 1
    if idx >= len(payload["turn_order"]):
        idx = 0
        payload["combat_round"] += 1
    # 跳过死亡/逃跑单位
    while not is_active(find_unit(payload, payload["turn_order"][idx])):
        idx = (idx + 1) % len(payload["turn_order"])
    payload["current_turn_index"] = idx
    payload["current_unit_id"] = payload["turn_order"][idx]
    new_unit = find_unit(payload, payload["current_unit_id"])
    new_unit["action_used"] = False
    new_unit["move_used"] = False
    # 返回 StateChange
```

**测试**：
- 移动到可达格成功 + position 更新
- 移动到不可达格失败
- 移动后 move_used=True，不能再移动
- end_turn 推进到下一个存活单位
- 回合结束时 combat_round 递增
- 跳过死亡/逃跑单位
- surprise round 中 surprised 单位被自动跳过
- 进入 round 1 时 surprised 标记全部清除

---

### Phase 4：攻击命令改造（~200 行修改）

**目标**：`combat_attack` 加入距离检查 + 地形效果 + 单位目标化。

**修改文件**：
- `combat.py` — 重写攻击逻辑：
  - 验证当前行动者 == current_unit_id
  - 距离检查：曼哈顿距离 ≤ weapon.range（+ 地形 range_bonus）
  - LoS 检查（远程攻击需要视线）
  - 地形 AC 加成（森林/石地）
  - 标记 action_used
  - **不再触发全怪物反击**（每个怪物有自己的回合）
  - 击杀检查 → 若全敌方死亡/逃跑 → combat_cleared
  - XP 结算复用现有 `_compute_combat_xp`

**关键变化**：
- 攻击不再自动触发 `_resolve_monster_responses`（这是旧模型最大的结构差异）
- 攻击者可以是任何 side 的单位（怪物攻击玩家/队友同样走此逻辑）

**测试**：
- 近战攻击距离>1 失败
- 远程攻击视线被墙阻挡失败
- 森林地形 AC+2 生效
- 攻击后 action_used=True
- 击杀最后一个敌人触发 combat_cleared
- XP 正确结算

---

### Phase 5：怪物规则 AI（~200 行新增）

**目标**：`battle_ai.py` — 规则 AI 决策（A* 寻路 + 地形偏好 + 目标选择）。

**新建文件**：
- `app/game_core/rules/battle_ai.py`

**修改文件**：
- `combat.py` — `combat_npc_turn` 命令：
  - 若当前单位是 enemy + 非精英 → 调用 `decide_monster_turn`
  - 执行返回的 move_to + action
  - 复用 Phase 4 的攻击/移动逻辑

**AI 决策树**：

```
1. hp_ratio < flee_threshold?
   └─ Yes → 计算远离所有敌人的最佳格 → move + flee
   └─ No → 继续

2. 评估所有敌方目标优先级
   └─ 因素：距离、HP、威胁等级（治疗者>输出者>坦克）

3. 选择最优攻击
   └─ aggressive: 最高伤害
   └─ cowardly: 最远射程

4. 能否原地攻击最优目标？
   └─ Yes → 不移动，直接攻击
   └─ No → A* 向目标移动
       └─ 近战：移动到目标相邻格
       └─ 远程：移动到射程内且偏好高地

5. 移动后能攻击？
   └─ Yes → 攻击
   └─ No → 结束回合
```

**测试**：
- cowardly 低 HP 时选择逃跑方向（远离敌人）
- aggressive 选择最高伤害攻击
- 远程怪偏好丘陵（有 range_bonus）
- 近战怪 A* 绕障到达目标
- 无法到达时移动尽量接近
- 已在攻击范围内不移动

---

### Phase 6：队友参战 + LLM AI（~200 行新增）

**目标**：队友从 PartySlice 进入战斗，LLM 驱动行为。

**修改文件**：
- `combat.py` — `combat_npc_turn` 扩展：
  - 若当前单位是 ally + companion → 调用 LLM AI
  - LLM 失败 → fallback 到规则 AI

**新增（应用层）**：
- `app/agent_orchestration.py` — `decide_companion_combat_turn()`
  - 构建战场 ASCII 摘要
  - 注入角色性格 + 关系上下文
  - LLM 返回 JSON → 解析 → 验证合法性 → 执行
  - fallback 规则 AI

**队友倒下处理**：
- `alive=False` → 跳过回合 → 战斗结束后不影响 PartySlice
- 不永久死亡

**测试**：
- 队友出现在 units 列表中（side=ally, source=companion）
- LLM 返回合法行动时执行成功
- LLM 返回非法行动时 fallback 规则 AI
- LLM 失败时 fallback 规则 AI
- 队友倒下后被跳过

---

### Phase 7：战斗地图模板 + 环境效果（~150 行新增 + 数据文件）

**目标**：预制地图加载 + 天气/时间效果。

**新建文件**：
- `app/game_core/content/battle_maps.py` — BattleMapRegistry
- `data/goblin_slayer/v2/battle_maps/*.json` — 10 个地图文件（各 2-3 变体）

**修改文件**：
- `combat.py` — start_combat 接入地图选择
- `battle_grid.py` — 环境效果应用（夜间/雨天/浓雾修正）

**数据文件示例**（`woodland.json`）：
```json
{
  "category": "woodland",
  "variants": [
    {
      "name": "林间小路",
      "size": [8, 6],
      "terrain": ["GGGGFGGG", "GGWWGGGG", "GGWWGFFG", "SSGGGFFG", "SSGGGGGG", "SSGGGGMG"],
      "player_spawn": [[0,3], [0,4], [1,4]],
      "enemy_spawn": [[7,0], [7,1], [6,2]],
      "legend": {"G": "grass", "F": "forest", "W": "water", "S": "stone", "M": "mountain"},
      "tags": ["forest", "water", "mixed"]
    }
  ]
}
```

**测试**：
- 地图加载 + 变体随机选择
- 环境效果正确修正命中/伤害
- spawn 点不重叠

---

### Phase 8：旧战斗代码清理 + SSE 事件（~100 行删除/修改）

**目标**：删除旧 participants 逻辑，完善前端通信事件。

**SSE 事件设计**：

```python
# 战斗初始化
SSEEvent("combat_started", {
    "grid": {...},
    "units": [...],
    "turn_order": [...],
    "current_unit_id": str,
    "environment": {...},
})

# 单位移动
SSEEvent("unit_moved", {
    "unit_id": str,
    "from": [col, row],
    "to": [col, row],
    "path": [[col, row], ...],  # 完整路径供动画
})

# 攻击结果
SSEEvent("unit_attacked", {
    "attacker_id": str,
    "target_id": str,
    "attack_name": str,
    "hit": bool,
    "damage": int,
    "target_hp": int,
    "target_alive": bool,
    "rolls": [...],
})

# 单位倒下
SSEEvent("unit_defeated", {"unit_id": str})

# 回合切换
SSEEvent("turn_changed", {
    "unit_id": str,
    "combat_round": int,
    "is_player_turn": bool,
})

# 战斗结束
SSEEvent("combat_ended", {
    "result": "victory" | "defeat" | "fled",
    "xp_awarded": int,
    "loot": [...],
})
```

**清理**：
- 删除 `_resolve_monster_responses`（每个怪物有自己回合）
- 删除 `player_flags`（per-unit 状态替代）
- 删除 `advance_combat_round`（自动推进）
- 删除 `participants` 相关逻辑
- 保留 `_apply_player_damage_resistance`（改名为 `_apply_damage_resistance`，通用化）
- 保留 `_compute_combat_xp`

---

## 五、文件影响总览

| 操作 | 文件 | 说明 |
|------|------|------|
| **新建** | `app/game_core/rules/battle_grid.py` | 网格 + 地形 + 寻路 |
| **新建** | `app/game_core/rules/battle_ai.py` | 怪物规则 AI |
| **新建** | `app/game_core/rules/combat_units.py` | 单位构建工厂 |
| **新建** | `app/game_core/content/battle_maps.py` | 地图注册表 |
| **新建** | `data/goblin_slayer/v2/battle_maps/*.json` | 10 个地图数据文件 |
| **重构** | `app/game_core/rules/handlers/combat.py` | 核心重构（~60% 重写） |
| **修改** | `app/game_core/state/slices/area.py` | payload 格式扩展 |
| **修改** | `app/game_core/orchestration/defaults.py` | 新命令注册 |
| **扩展** | `app/agent_orchestration.py` | 队友战斗 LLM AI |
| **修改** | `data/goblin_slayer/v2/monsters.json` | 补充 speed 字段 |

### 不动的文件

| 文件 | 原因 |
|------|------|
| `status_effect.py` | 效果系统无需改动，active_effects 格式兼容 |
| `skill_check.py` | d20 机制复用，无需修改 |
| `proficiency.py` | 不变 |
| `spell.py` / `spell_effects.py` | 后续 Phase 做，本次不动 |
| `encounter.py` | 遭遇→战斗的触发逻辑不变，只是 start_combat 的 payload 变了 |

---

## 六、MonsterTemplate 需补充的字段

当前 `monsters.json` 需为战旗化补充：

```json
{
  "goblin": {
    "speed": 3,              // 新增：移动力（格数）
    "threat_level": "minion", // 新增：minion | elite | boss（决定 AI 类型）
    // 已有字段沿用：
    "attacks": [{"range": 1, ...}, {"range": 10, ...}],
    "ai_personality": "cowardly",
    "flee_threshold": 0.3,
    "flee_chance": 0.6
  }
}
```

**CharacterTemplate 需补充**（队友参战用）：

```json
{
  "priestess": {
    "combat_stats": {          // 新增
      "hp": 18,
      "max_hp": 18,
      "ac": 14,
      "stats": {"str": 8, "dex": 12, "con": 12, "int": 13, "wis": 16, "cha": 14},
      "speed": 3,
      "attacks": [
        {"name": "圣锤", "hit_bonus": 2, "damage_dice": "1d6", "damage_type": "bludgeoning", "range": 1}
      ],
      "skills": ["miracle_heal"]  // 可用技能列表
    },
    "combat_style": "优先治疗受伤的队友，保持后排位置"  // LLM prompt 用
  }
}
```

---

## 七、风险与约束

### 7.1 兼容性风险

- **旧存档**：旧战斗 payload 无 grid/units → 需要版本标记 + 旧逻辑暂时保留（Phase 2 兼容期，Phase 8 清理）
- **前端同步**：后端 payload 变化需前端配合（SSE 事件格式全变）→ 建议后端先做完，前端再适配
- **45 个旧测试**：大部分需要重写（payload 格式变了）→ 每个 Phase 同步更新测试

### 7.2 性能考量

- A* 寻路在 8×6 网格上 < 1ms，无性能问题
- 每轮 2-3 次 LLM 调用（队友 + 可能的精英怪）→ 与现有对话系统同量级
- BFS 可达性计算同理，8×6 网格开销忽略不计

### 7.3 规模评估

| Phase | 预估新增/修改行数 | 预估测试数 |
|-------|----------------|-----------|
| Phase 1 | ~250 新增 | ~15 |
| Phase 2 | ~300 修改 | ~12 |
| Phase 3 | ~250 修改 | ~10 |
| Phase 4 | ~200 修改 | ~10 |
| Phase 5 | ~200 新增 | ~10 |
| Phase 6 | ~200 新增 | ~8 |
| Phase 7 | ~150 新增 + 数据 | ~6 |
| Phase 8 | ~100 删除/修改 | ~5 |
| **总计** | **~1650 行** | **~76 测试** |

每个 Phase 符合单次 agent 体量限制（~300 行新增 + ~200 行修改）。

---

## 八、开放问题（待确认）

1. **队友 combat_stats 数据源** — characters.json 现有模板有 `level` 但无 hp/ac/stats，需要补充。数值如何确定？（按 D&D 5e 职业表推算？手写？）
2. **AoE 攻击** — 法术系统（spell.py）的 AoE 是否在本次重构范围内？还是作为后续 Phase？
3. **机会攻击** — D&D 5e 的 opportunity attack（离开近战范围触发反击）是否实现？增加复杂度约 +100 行
4. **防御/闪避行动** — 旧的 defend/disengage/dash 是否保留为战斗行动选项？还是简化为只有攻击/技能/物品/逃跑？
5. **战斗中存档** — 当前战斗状态完全在 hostile_tracking 中，存档/读档天然支持。但需确认前端是否需要特殊处理
6. **多地图变体数量** — 10 类地图 × 2-3 变体 = 20-30 个地图数据，是否足够？还是先做 3-5 类核心地图？
